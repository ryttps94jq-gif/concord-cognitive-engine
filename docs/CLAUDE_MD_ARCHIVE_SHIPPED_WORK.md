# CLAUDE.md archive — Recent shipped work (most → least recent)

Moved out of `CLAUDE.md` on 2026-09-07 purely to keep that file under a size budget (target <150KB) — no content was rewritten, this is a verbatim relocation of the full changelog table. `CLAUDE.md` now carries only a short pointer here. Numbered claims in this file are still covered by `scripts/check-doc-claims-all.mjs` (it scans `docs/*.md`).

---

## Recent shipped work (most → least recent)

| Commit | What landed (every claim below was verified by `ls`/`grep` against the working tree at HEAD) |
|---|---|
| Codebase audit + prod-readiness pass (2026-07-31, branch `claude/codebase-audit-deployment-n0g9ba`, PR #875) | Full audit (deployment, connection reliability, LLM pipeline, security, UI) with fixes landed in the same branch, not just findings. **Deploy**: `startup.sh` now copies `.next/static`+`public/` into the standalone build (frontend was previously visually dead — every `/_next/static/*` 404'd); fixed the `--runpod`/`--cloudflare` env-flag docs mismatch; rewrote Cloudflare ingress templates (root→frontend :3000, explicit `/socket.io/*`+`/godot-ws` rules); renamed the tunnel-token env var to match what `startup.sh` actually reads; added a bare-metal env template + bootstrap script. **Connections**: deleted the duplicate ungated 120s state-saver that was defeating the mutation-seq-guarded 5-min one (~40GB/day write amplification); debounced `saveStateCritical` and dropped its forced `global.gc()`; raised the Godot gateway's ping reaper to match socket.io's 60s; fixed `ConnectionStatus.tsx` to read the real socket signal instead of a decorative `/health` poll. **LLM pipeline**: moved the embedding model off the conscious-brain instance (was evicting the 14B model on every embed call); routed streaming chat through the same queue/circuit-breaker/BYOK path as `ctx.llm.chat` (closing a ~50-line duplicate fallback); now sends `num_ctx` on every Ollama call (prompts were silently truncating at Ollama's 2-4k default against a 32k token budget); added an ongoing brain health re-probe loop. **Private Mode / High Power Mode**: new opt-in per-account LLM routing (`users.brain_mode`, migration 397) — Private (default) never leaves local Ollama brains, whole-account guarantee across all 4 dispatch paths (`ctx.llm.chat`, `callBrain`'s ~60 call sites, streaming chat + its fallback, `byo-router.js`); High Power routes through a per-slot platform-provider registry (Groq default for utility/subconscious/repair — verified no-train on any tier; Gemini default for conscious/vision — free tier trains, disclosed plainly in the onboarding/settings copy; Mistral as an operator-configured override), gated behind `CONCORD_HIGH_POWER_ALLOWLIST`, with a deterministic outbound secret/PII scrubber and a fail-closed per-(provider,slot) budget limiter. **Security** (found via a dedicated code-level pass, not the Aikido dependency scan — all now fixed): SEC-1 CRITICAL — authenticated RCE in `domains/invariant.js` (an AST-allowlist validated one expression string while `new Function` compiled a different, regex-spliced one; a string value could break out of its `JSON.stringify` quoting and execute); SEC-2 — SSRF in `cooking.import-from-url` + `productivity.calendar-import-ics` (bare `fetch()` on a caller-supplied URL, no SSRF guard); SEC-3 — privilege-escalation-shaped RBAC role-update endpoint with no admin gate; SEC-4 — open redirect (+ a lower-severity token-key-name leak) in the OAuth callback's `redirect` query param; SEC-5 — path traversal in whiteboard's publish-as-blueprint. **CI honesty**: the Trivy container-CVE gate had never actually scanned anything (it checked for a root `Dockerfile` that doesn't exist — the real ones are `server/Dockerfile`/`concord-frontend/Dockerfile` — and silently no-op'd green every run); fixed, plus SHA-pinned third-party actions and added least-privilege `permissions:` to 9 workflows that had none. **Test-suite root causes** (not just retries): the tick-SLO 503 flake was the front-door event-loop-lag shedder tripping during the governor's first heavy tick, not a bcrypt race as first assumed — fixed by requiring a sustained multi-sample lag-settle streak before driving load; the Integration Test job's 3 adversarial-auth failures were the same shedder hitting an externally-started CI server that was missing `CONCORD_LOAD_SHED_ENABLED=0` (a self-spawned test path already had it); the 10 "E2E Core" failures were traced to two stacked bugs — mocked Playwright routes never answering the CORS preflight axios's default `Content-Type` header forces, AND (found second, by actually reproducing locally against the sandbox's pre-installed Chromium) the frontend's own CSP `connect-src` directive lacking an `http:` scheme entry, independently blocking the CI topology's plain-http cross-origin calls before CORS was even relevant — both fixed, all 10 verified passing locally. **UX-polish**: a since-superseded mid-session claim of "~49 lenses still generic" didn't survive a live `node scripts/grade-ux-polish.mjs --honest` run — real count was 3 (creative-writing, eco, game-design), each already rebuilt onto a genuinely deep bespoke UI but still importing the now-redundant generic scaffold pieces alongside it; stripped them from all 3 page shells (same pattern as the pre-existing `market` fix). Score: 0.995 → **1.000**, 265/265 polished, 0 generic scaffolds — see the UX-polish snapshot in the invariants section above. |
| ConKay prod-audit + money fix (2026-06-07, branch `claude/conkay-prod-audit-B3Mdk`) | Production-readiness pass. **🔴 Critical money bug fixed** — `economy/balances.js#getBalance` (and 6 other credit summers) double-credited the recipient of every TRANSFER/MARKETPLACE_PURCHASE (the two-row debit+credit pattern's debit row carries `to_user_id`), minting CC on every transfer/sale and breaking the 1:1 USD peg. Surgical read-side fix via `CREDIT_ROW_PREDICATE` (excludes the redundant debit-half; royalty/emergent/reversal still credit) + `tests/economy/ledger-conservation.test.js` proving conservation. **Earned-only CC withdrawals** — `withdrawals.js#earnedWithdrawableBalance` (shared by `requestWithdrawal` + the creator-dashboard surface): only marketplace-sale + royalty CC is withdrawable (purchased CC is closed-loop store credit — the Roblox DevEx shape, money-transmitter risk-reducer); Stripe payout idempotency key; `SESSION_SECRET` ≥32 gate. **Front door** — onboarding "Skip" now persists (killed the welcome modal that bled through on every surface), overlay sequencing, GL-crash → 2D fallback. **ConKay** — cross-browser STT fallback (`lib/voice/mediarecorder-stt.ts` → `/api/voice/transcribe-raw`), persistent summon button, verifiability trust badge (Grounded vs "Reasoned—verify"), **compute-don't-guess math** (routes computation to the real `domains/math.js` CAS via `ctx.runMacro`), semantic archive search (`discovery.search` embedding rerank + keyword fallback). **`reason.verify`** macro — LLM-as-judge: deterministic citation-resolution floor (catches fabricated citations, `tests/reason-verify.test.js` 5/5) + multi-brain council judge when brains are up. **faction:strategy-move** now surfaces in the EmergentEventFeed; `.env.runpod` `OLLAMA_NUM_PARALLEL` OOM footgun fixed. New tests: ledger-conservation 4, withdrawal-earned-policy 8, semantic-search-fallback 4, reason-verify 5, creator-dashboard-ledger 5 (all green). |
| Living Society P0 (2026-05-29, branch `claude/continue-plan-nkNAQ`) | The grounded resource + crafting substrate so UGC (spells/items/food/buildings) is balanced, not "imagine an OP spell." **Part 1** — mig 278 `resource_properties` (+ `player_inventory.properties_json` per-slot override) + `lib/resources.js` (canonical `RESOURCE_CATALOG` source-of-truth: 5 rarity tiers + magical sub-tier soul-gem/mana/aether/essence; `propsFor` override→DB→catalog→default; `seedResourceProperties`). **Part 2** — `lib/craft-resolve.js#resolveCraft`: the single deterministic resolve — output potency = weighted input potency + skill + station + magical-fuel bonus; dominant affinity cascades from the highest-potency input; CONFLICTING affinities lower stability (Concordia twist on BotW cancel) → seeded-hash backfire; potency floor gates god-tier; failure is SOFT (wasted mats + minor debuff, never throws); maps potency → `executeCraft` `qualityMultiplier` [0.5, 2.0]. **Part 3 (this session)** — wired it into the live path: `executeCraft` (which the `/api/crafting/execute` route AND `cook-engine` both flow through) now DERIVES quality from input resource properties via `resolveCraft` when no explicit minigame multiplier is passed; stamps `resource_affinity`/`potency`/`stability` provenance onto the output DTU; applies the soft backfire/fizzle debuff to `user_active_effects`; per-slot `properties_json` overrides resolve hotter (crossbreed-drop hook). `seedResourceProperties` seeded at boot by the content-seeder. Kill-switch `CONCORD_CRAFT_RESOLVE=0`; dials in `docs/BALANCE_DIALS.md` (`CONCORD_CRAFT_*`). **Part 4 (this session)** — extended the "wrap, don't rewrite" to 3 of the plan's 5 systems: `tool-tree.js#craftTool` (tool quality now scales with the consumed materials' properties, non-blocking) + `glyph-spells.js#mintSpell` (optional power-source FUEL — soul gems/mana/aether — amplifies the spell potency-proportionally per the Fireball I→V gradient + is consumed from inventory; no fuel = byte-identical; dial `CONCORD_SPELL_FUEL_BOOST`). The remaining two (`skill-evolution.applyEvolution` + multi-step `craft-chains`) carry no structured resource inputs yet — flagged as the Phase 0 tail in `docs/LIVING_SOCIETY_PLAN.md`. Contract tests: `tests/resources.test.js` 11, `tests/craft-resolve.test.js` 9, `tests/craft-engine-resolve-wire.test.js` 7, `tests/craft-resolve-wire-extra.test.js` 7 (34 total green; glyph/tool/cooking/eco regressions green). |
| Depth & Balance continuation (2026-05-29, branch `claude/audit-findings-remaining-BkcKy`) | Completed the remaining `docs/DEPTH_BALANCE_PLAN.md` items + folded in the verified-real `docs/POLISH_AUDIT.md` (from main) findings. Each item code-verified first (docs treated as stale) + a contract test, 0 regressions. **D5** — CK3 hooks: mig 277 `npc_hooks` + `server/lib/hooks.js` (held/spendable/expiring/inheritable leverage from secrets; wired into proposeScheme block+success-bonus, `interveneInScheme` new `blackmail` branch, `onNpcDeath` inheritance, secret-discovery macros, `GET /api/npc/:npcId/hooks` + NPCTraitInspector, `hook-decay-sweep` @240). **D6** — run-mode payout-on-loss: shared `run-difficulty.js#grantRunMeta` (banks the `roguelite_meta_currency` gem bank), roguelite × tier loot-mult floored at 1.0, horde pays wave×8+kills×0.25 on every end, extraction extract pays + death consoles, new `extractionDanger` (`GET /api/extraction/:runId/danger`). **E3** — fixed the missing `skill:evolution-available` emit + deterministic "Arise" `skill:evolved` beat (`composeEvolutionBeat`); faction E→S `rankLetterFor`/`tierToRank` + `reputation:rank-up`; juice-bridge listeners. **E4** — `spouse-reactivity.js` (married NPC reacts to player factions/kills/schemes/death, estranges past threshold; wired into NPC-kill path + `romance.spouse_react`). **D4#3** — wired `seedStarterGear` into `persistGeneratedNpc` (procedural NPCs now visually distinct + lootable). **D4#5** — `secrets.seedSecretForNpc` + spawner promotes a deterministic fraction (`CONCORD_PROCGEN_SECRET_FRACTION` 0.33) of procedural secrets into the discoverable `secrets` table (surveillance→hook→quest-gate). **E5** — restaurant Diner-Dash batching combo (tip multiplier). **E0** — `server/lib/client-config.js` + `GET /api/config/client` + `useClientConfig` hook: ~24 frontend POLL_MS/throttle dials now env-tunable without a rebuild (DriftAlertToast + RestaurantDashboard migrated as reference). **Polish-audit fixes (all verified real):** T0.1 🔴 code puzzles unsolvable (editor `{op,a,b}` vs VM `{dst,src,to}` — `_normalizeInstr` adapter); T0.2 🔴 all station/HUD audio silent (underscored sfx ids vs hyphenated SFX_MAP — `SFX_ALIASES`/`resolveSfxId`); T1.1 dialogue LLM-off cliff (`composeDeterministicDialogue` fallback); T1.2 trivia unwinnable (`getAnswerChoices` multiple-choice); T1.5 hacking memory-test (`hintForStep`/`getHint`/`nextHint` guided trail). **Verified STALE — already done (meta-finding holds):** `music.ai-playlist` already LLM-wired; T3.1 faction-strategy already emits + `StrategicWarBanner`/`EmergentEventFeed` consume it; C-series every world already has 10–33 lore items (tunya 33). Contract tests: hooks 22, run-mode-payout 12, e3 8, spouse 9, gear 3, secret 5, restaurant-combo 4, client-config 4, code-puzzle 8, trivia 6, dialogue 7, hacking 8 (80 new, all green; 219 touched-area regressions green). |
| Game-plan completion (2026-05-28, branch `claude/game-plan-completion-2a0KH`) | Completes the `docs/CONCORDIA_PLAN.md` T-series end-to-end (the plan landed on main mid-sprint; both the plan's exact specs and a few bonus interpretations shipped). **T1.4b** — server-authoritative combat feel: `lib/combat/impact-feel.js` maps T1.4a poise severity → hitstop/knockback/wince on a new `combat:impact` event; `CombatImpactFeelBridge` consumes it. **T3.1** — `lib/skills/skill-mastery.js` (mastery tiers novice→grandmaster + per-skill VFX scaling) + `/api/crafting/skills/mastery[/:skillType]` + `useSkillMastery` hook; combat:impact carries the mastery-scaled vfx descriptor. **T2.1** — two weaponise consumers: authored `narrative_context.weaponise_at` (mig 261 `weaponise_triggers` + befriend/expose firing) AND the secrets-domain `secrets.weaponised_holder_at` (mig 263; NPCs autonomously open blackmail schemes along held-secret edges via `proposeScheme(motive:'secret')`). **T2.2** — InheritanceLog in `TombsOverlay` + `npc_legacy.inheritance_from_deceased` (the cross-time "who inherited what" thread, previously reading non-existent columns). **T2.3** — `lib/scheme-overhear.js` + `scheme-overhear-cycle` heartbeat (proximity overhear feeds discovery) AND `POST /schemes/:id/intervene` (expose/abet/ignore barge-in) + richer overhear payloads. **T2.4** — `scripts/audit-emergent-wiring.mjs` (CI-guarded): 87 wired / 104 entity-inline / 0 orphan; wired the one real orphan `population-migration-cycle`. **T3.3** — two zone systems: per-world skybox sun-disk + building silhouette (`concordia-theme.ts` `sunDiskForWorld`/`buildingStyleForWorld` → SkyWeatherRenderer + BuildingRenderer3D) AND a `world_zones` combat-region substrate (mig 262: safe/sanctuary/pvp/lawless/hazard, `world-zone-hazard-cycle`, `ZoneBadge`). **T3.4** — adopted restaurant tips 0.20/0.15 + mahjong rarity re-weight (iipeiko 200→100, pinfu 100→250, ittsuu 500→700) + new combat/intrigue/zone balance dials env-overridable. **BUG B** — socket `combat:hit` now carries element/skillId/weapon/tier/style (element bursts fired). **T0** — this CLAUDE.md (migrations 258→262, heartbeats 68→103, restaurant + mahjong + combat-impact lines corrected) + `docs/BALANCE_DIALS.md` + authored the missing `content/festivals/concord_rising.json`. Every item has its own integration test (combat-impact-feel, skill-mastery, weaponise-triggers, secret-weaponise-npc, scheme-overhear, scheme-intervene, npc-inheritance-from-deceased, emergent-wiring-audit, world-zones, balance-dials, mahjong-value-balance + the concordia-zone-architecture vitest). |
| Phase Z — Lockdown (2026-05-28, branch claude/concurrency-threading-plan-H2zCN) | Production-readiness cleanup of the 15 gaps left by Phase D. **Z1**: 5 Phase-D sidebar lenses registered in `lens-registry.ts` + `lens-manifest.js` so Ctrl+K palette + score-lenses resolve them (courtship, fishing, creatures, garage, reasoning-traces). **Z2**: boot-time content seeding for 4 empty substrates — `content-seeder.js` now invokes `seedDefaultGlyphLibrary` + walks `content/hacking-puzzles.json` (10) + `content/code-puzzles.json` (8) + `content/trivia-questions.json` (30 with answer DTUs minted) + `content/karaoke-songs.json` (10 songs, served via new `/api/karaoke/songs`); each pass idempotent on name. **Z3**: 16 authored NPCs + 5 factions + 6 lore events for `content/world/concordia-hub/` (the landing world that previously had zero people). Includes cross-world resonance refs to tunya / fantasy / cyber / sovereign-ruins / superhero / lattice-crucible / crime; embeds the dome-stabilisation plot anchor + Asbir-Iyatte sealed record + the impossible-print 3-clue chain (Kiren + Orin + Kel). **Z4**: POST alias for `/api/connective-tissue/search` (feed + social lenses) + 5 personas-domain action stubs (`get`, `stats`, `versions`, `publish`, `install`) — `get` + `stats` thin wrappers, the rest return `{ok:false, reason:'roadmap'}` so the lens renders "coming soon" badges instead of crashing. **Z5**: audit of the `tools.web_search` "duplicate" claim — direct grep showed only one registration in current code (the prior Phase K removal at server.js:24613 had already resolved the dup). Confirmed `jobs.list` was also stale. Kept the canonical registration with an audit-note comment. **Z6**: 7 logger argument-order inversions fixed across content-seeder, voice-synthesis, city-presence, combat/{faction-war, flow-engine, flow-recorder} — all now use canonical `logger.x("source", "message", { meta })`. The FATAL/JWT_SECRET claim was wrong on audit (process.exit(1) fires correctly on that path); no change. **Z7**: shared `lib/concordia/juice.ts` dispatch helpers (menuOpen, successJuice, milestoneJuice, failureJuice, sfx) wired into the 11 station overlays via `_StationOverlayShell` (built-in fade+scale entry + menu-open SFX), 10 standalone HUDs (slide-in classes in `globals.css`), and key user actions on 9 components (plant/harvest, serve, trivia correct/wrong, hack step/complete/reset, code test pass/submit, karaoke grade S/A vs B/C, mahjong score, glyph mint, hybrid creature, climb summit, brawl invite/accept). No new dependencies — Tailwind keyframes + plain CSS. **Z8**: deferred (no real balance pass yet — constants documented in this CLAUDE.md). **Z9**: 5 React Native screens (CourtshipScreen, FishingScreen, CreaturesScreen, GarageScreen, ReasoningTracesScreen) registered in `AppNavigator.tsx` Stack with deep-link routes. In-world HUDs deferred until a mobile world lens exists. **Z10**: 4 new integration tests under `server/tests/integration/` covering DC1 sports-engine end-to-end (open league → 4 teams → schedule → play → standings), DB9 party-combat findActiveSessionForPlayer + tick + state, DB12 factory live-update (place → connect → tick → item moves), Z2 boot seeders. 83/83 passing. Main npm test suite re-ran clean (20,495 pass / 23 pre-existing behavior fails / exit 0). **Z11**: this CLAUDE.md — lens count 236→253, migrations 201→258, NPCs 112→128, "Missing" cleared. |
| Concordia game-feel pass (May 2026, branch claude/audit-concordia-systems-HaLiM, PR #317) | Six surgical themes + five deferred follow-ups against the open-world research spec. **Theme 1 — audio coupling**: `signals.js` recordSignal emits `world:sonic-pulse` for sonic_os.ambient_db>5 from non-sensor sources; `SoundscapeEngine.tsx` adds `setAmbientVolume` + `concordia:sonic-pulse` handler that pulses master gain 0.65–1.0 by magnitude; `footstep-mud-squelch` SFX for high-humidity terrain (8/8 tests). **Theme 2 — fauna boids**: `fauna-spawner.js` `clusterCenterFor(world, biome, species)` deterministic from sha1; `creature-behaviors.js` `tickFlock` implements separation/alignment/cohesion + flee-from-player; new heartbeat `creature-flock-cycle` (frequency 4) with in-memory `STATE.creatureMotion` + bulk-flush DB writes (13/13 tests). **Theme 3 — chemistry-cascade**: `signal-propagation.js` propagateThermal (8-neighbour spread gated by humidity + flammable building), propagateMoisture (rain bumps dry cells), evaluateCombos (steam at hot+humid, smoke at very-hot, evaporate at hot+humid+very-hot, poison cleanse on steam), propagateLightningChain (combat-inline; wet ground + lightning → CHAIN_MAX_TARGETS=5 nearby targets at ×0.4 magnitude); new heartbeat `signal-propagation-cycle` (frequency 3); migration 148 indexes; combat:chain socket fan-out (21/21 tests). **Theme 4 — diegetic UI**: `QuestTracker.tsx` default-breadcrumb mode (next-step only, J toggles full list, localStorage `concordia:questTracker:mode` persists; focus-aware); `NPCActivityTag.tsx` floating emoji icon above NPCs within 12m (training/patrol/trade/craft/socialize/commune/sleep/rest); GET /:worldId/npcs LEFT JOINs npc_routine_state (15/15 tests). **Theme 5 — combat juice**: `physics-world.ts` `knockbackKinematic` velocity-integration through moveCharacter (kinematic capsules ignore applyImpulse); GameJuice fires `concordia:hit-pause` (80ms target / 50ms attacker on heavy/crit; 200/60 on kill) + `concordia:knockback`; AvatarSystem3D mixer.update(0) freezes during pause; `DamageBillboard.tsx` 3D-anchored damage numbers using projector pattern, 32-cap, ttl auto-cull, kind-driven colour; server combat:hit emit includes targetPosition + attackerPosition (8/8 tests). **Theme 6 — movement**: kinematic vertical-velocity integration with gravity + glide (descent ≤ −1.5 m/s + 8% horiz boost) + swim (buoyancy + reduced gravity); `requestJump`/`setGlide`/`setSwim`/`isAirborne`/`isGrounded`/`isGliding`/`isSwimming` API; per-world `registerWaterPlane(worldId, y)` registry; AvatarSystem3D Space=jump-then-glide focus-aware; Y-clamp bypassed when airborne. **Deferred D1** — water plane: ConcordiaScene registers waterY=2.0 + draws river-bluff + Fall Kill Creek strips. **Deferred D5** — Simplex terrain: `simplex-noise.ts` (xorshift32-seeded 2D simplex + octaveNoise2D); TerrainRenderer replaces sine-wave noise with 4-octave fractal at three frequency bands. **Deferred D4** — async signs: migration 149 + `player-signs.js` (placeSign/signsNearby/mySigns/removeSign/cleanup; MAX_ACTIVE_PER_USER=50, PLACE_COOLDOWN_S=60, MESSAGE_MAX_LEN=80, SIGN_TTL_DAYS=7); 5 macros; `WorldSigns.tsx` projector overlay + B-key picker; `world:sign-placed` realtime; new heartbeat `player-signs-cleanup` (240); 18/18 tests. **Deferred D3** — hidden quest triggers: migration 150 (quest_triggers + quest_trigger_visits with composite PK); 6 trigger kinds (proximity/visits/dialogue/item_handover/time_window/world_state); `evaluateTriggersAtPosition` with TIME_PROXIMITY_S=30 debounce; `fireTrigger` respects requires_visits + max_fires_per_user; 6 macros (12/12 tests). **Deferred D2** — Dark Souls shadow-corpse: migration 151 (player_corpses); `player-corpse.js` `dropCorpseOnDeath` (25%×wallet capped at 1000, debits user_wallets, marks prior active corpse lost), `recoverCorpse` (RECOVER_RADIUS_M=4m, owner-only, credit refund); 3 macros; combat-kill path drops corpse for player deaths only (skipped in training matches); 12/12 tests. **Total**: 84/84 new server tests + 23/23 new frontend tests, 0 TypeScript errors. Avoids PR #316 Phase 6 territory (mounts, new worlds, synthetic playtest, mount IK, onboarding arc). |
| Phase 6d — Mobile Parity (May 2026, commit 3f1fa3a) | `concord-mobile/src/api/macro-client.ts` (~150 LOC, MacroClient class with bearer-auth + transient-error retry; configureMacroClient + getMacroClient singleton; domain wrappers: Beats, LandClaims, GlyphSpells, Discovery, KnowledgeTrade, DtuPortability) + `hooks/useBeats.ts` (30s poll fallback) + `hooks/useDiscovery.ts` (debounced search) + `__tests__/macro-client.test.ts` (6 jest tests pinning the contract). |
| Phase 6c — Cross-lens Discovery (May 2026, commit b5542db) | `server/lib/cross-lens-discovery.js` (searchDtus with title+meta_json LIKE; getKindFacets; getTrending via dtu_citations group-by; MAX_RESULTS=100; query length [2,200]; personal-scope privacy gate) + `domains/discovery.js` (search/facets/trending macros) + tests (10/10). |
| Phase 6b — DTU Portability / Export (May 2026, commit f1ccd38) | `server/lib/dtu-portability.js` (canonicalStringify deterministic JSON; exportUserCorpus packs DTUs + dtu_citations + economy_ledger with SHA-256 integrity hashes; envelope spec='concord-dtu-pack/v1' with instance_signature; validateEnvelope detects tampering via hash mismatch; importEnvelope idempotent on dtu.id) + `domains/dtu-portability.js` (export/validate/import macros) + tests (10/10). |
| Phase 6a — Forge → Marketplace (May 2026, commit bd07d09) | `server/lib/forge-marketplace.js` (mintForgeAppAsDtu writes kind='forge_app' DTU + registers citation against template; listForgeAppsForUser). **Correction (2026-07-24 grounding-audit continuation):** the original `listForgeAppOnMarketplace` ("tries creative_artifact_listings v2 then falls back to marketplace_listings v1") was functionally hollow — no migration anywhere ever creates `creative_artifact_listings` (a standing honest-suppression in `scripts/audit/gates/schema-drift.mjs`), and `marketplace_listings` has real readers (`server/durable.js`, `server/guidance.js`) but no purchase route, so a "listed" forge app was never actually purchasable. Fixed: it now lists through the real `marketplace.list`/`purchaseWithRoyalties` macros (the same `dtu.marketplace` system the Creator lens's Listings tab uses), building on the `resolveMarketplaceDtu` SQL-shadow-hydration fix that makes a raw-INSERT forge DTU resolvable to those macros. Proof: `node --test server/tests/forge-marketplace.test.js server/tests/forge-marketplace-purchase-e2e.test.js`. |
| Phase 5e — Procgen Wilderness (May 2026, commit acb64a0) | Migration 137 (procgen_regions UNIQUE on drift_alert_signature + procgen_region_visits); `server/lib/procgen-regions.js` (DRIFT_TO_REGION mapping for 6 drift types → 5 region kinds: haunted_glade/corrupt_market/hollow_chamber/overgrown_wild/silent_field; REGION_BIASES per kind on temp/humidity/light/airQuality/sonic; generateRegionFromAlert deterministic anchor from sha1(signature) in 2km×2km box; regionAt point-in-circle; applyRegionBiases layers on top of base signals; recordRegionVisit; decayRegion); lattice-quest-cycle wired to also spawn region; lattice-quest-composer realiseLatticeBornQuest cascades into decayRegion; tests (15/15). |
| Phase 5d — Magic Glyph Composition (May 2026, commit 2a70113) | Migration 136 (glyph_components + player_glyph_spells); `server/lib/glyph-spells.js` (10-entry seed library using actual base-6 algebra Unicode chars ⟐⟲⊚ + composites; composeSpell folds chain via glyphAdd; harmonic damage amplification by chain length; dominant element by majority; mintSpell writes kind='spell_recipe' DTU + player_glyph_spells row); `domains/glyph-spells.js` (5 macros); tests (14/14). |
| Phase 5a — Player Settlements + Land Claims (May 2026, commit be80a06) | Migration 135 (land_claims + land_claim_invites + land_claim_events); `server/lib/land-claims.js` (claimLand with radius clamp [5,200] + overlap rejection + walletDebit guard; inviteToClaim with co_owner/guest/tax_collector roles; tickMaintenance daily decay; topUpBond owner-only; claimAt point-in-circle; canActIn permission check with trespass_check event); `emergent/land-claims-cycle.js` heartbeat freq 240 (~1h); `domains/land-claims.js` (6 macros); tests (19/19). |
| Phase 5c — Seasons + Long-cycle Time (May 2026, commit 163d72e) | Migration 134 (world_seasons + season_events); `server/lib/seasons.js` (6 seasons × 7 days = 42-day Concordia year; SEASONS table with tempBias/humidityBias/lightBias; seasonalBias + seasonalYieldMultiplier where deep_winter herb 0.2× and harvest 1.3× general; advanceSeasonForWorld idempotent with 'season:enter'/'season:transition'/'year:begin' events; emits 'world:season-transition' realtime); `emergent/season-cycle.js` heartbeat freq 480 (~2h); tests (13/13). Aliasing bug pinned: capture priorYear before UPDATE because some adapters return row references. |
| Phase 5b — Death + Legacy (May 2026, commit ff83f17) | Migration 133 (npc_legacies UNIQUE on npc_id + npc_inheritance_links); `server/lib/npc-legacy.js` (composeLastWords deterministic from sha1(npc.id + cause) with 5 cause-specific pools; findHeirs priority children > same-archetype faction-mates > any faction-mate, capped at 3; inheritGrudges/Preoccupations/Desires/Recipes/Wealth; onNpcDeath idempotent main entry; getLegacy + getTombsForWorld + getInheritanceForHeir read paths); npc-consequences.js fires onNpcDeath in fire-and-forget IIFE post UPDATE is_dead; tests (14/14). |
| Phase 4c — Lattice-Born Quests (May 2026, commit 7d86c56) | Migration 132 (lattice_born_quests UNIQUE on drift_alert_signature); `server/lib/lattice-quest-composer.js` (alertSignature day-bucketed; QUEST_TEMPLATES for 6 drift types each 3-step investigate→confront→resolve; pickHostNpc archetype-matched + deterministic; persistLatticeBornQuest idempotent; spawnQuestFromAlert end-to-end; realiseLatticeBornQuest); `emergent/lattice-quest-cycle.js` heartbeat freq 180 (~45min) drains drift alerts into quests; tests (28/28). |
| Phase 4b — Living Economy (May 2026, commit 96295a0) | Migration 131 (npc_inventory + economy_flows + regional_scarcity); `server/lib/npc-economy.js` (RAW_RESOURCES + FINISHED_GOODS each 8 items; ARCHETYPE_GATHER_TARGETS + ARCHETYPE_CRAFT_RECIPES; performGather + performCraft + performTrade + consumePersonalNeeds; computeRegionalScarcity 1h window clamped [-0.5, 1.0]; refreshScarcityCache; priceModulator 1.0 + s × 0.5; dispatchEconomicAction); `emergent/npc-economy-cycle.js` heartbeat freq 8 (~2min); npc-marketplace.js scarcity-aware pricing via priceForRecipeWithScarcity; tests (24/24). |
| Phase 4a — NPC Daily Lives (May 2026, commit a00f90f) | Migration 130 (npc_schedules + npc_routine_state); `server/lib/npc-routines.js` (composeScheduleForNpc deterministic from sha1(npc_id + day_seed + preoccupation_signature) returns 8 blocks; archetype routines for warrior/scholar/trader/mystic/guard/healer/hunter/default; preoccupationOverrides for war/rebuild/expand/isolation/personal_loss; persistScheduleForNpc idempotent; advanceRoutine block transition + position nudge + ARRIVAL_RADIUS_M=4m + SIGNAL_EMIT_INTERVAL_S=120s; ACTIVITY_SIGNALS for 10 kinds; regenerateSchedulesForFaction Phase 2 cascade); `emergent/npc-routine-cycle.js` heartbeat freq 5 (~75s); npc-asymmetry.js refreshFactionPreoccupations now cascades into regenerateSchedulesForFaction; routes/worlds.js dialogue endpoint reads npc_routine_state for current_activity prompt context; tests (24/24). |
| Phase 3 — Personal Beat Scheduler (May 2026, commit c3674c2) | Migration 129 (player_beats); `emergent/personal-beat-scheduler.js` heartbeat freq 60 (~15min) — TTL expiry 24h, confidence×novelty pick, kill-switch CONCORD_PERSONAL_BEATS=0; realiseBeat cascades into forward-sim + bumps concordia_alignment +0.05 on realised / refusal_debt +0.02 on rejected; findOpenBeatBySubject + listBeatsForUser; `domains/beats.js` (list/realise/find_open_for_subject macros); `concord-frontend/components/world/PersonalBeatWidget.tsx` floating card + carry/refuse/dismiss; quest-engine + dialogue endpoint hook for realisation; tests (17/17). |
| Phase 2 — NPC Asymmetry (May 2026, commit 02674fe) | Migration 128 (npc_grudges + npc_preoccupations + npc_desires); `server/lib/npc-asymmetry.js` (seedNPCAsymmetry idempotent from sha1; refreshFactionPreoccupations Phase 11 cascade; recordPlayerImpactEvent IMPACT_SEVERITY table where killed_by_player=8 saved_by_player=-7; findOfferedDesire archetype-matched per playerMetrics; composeAsymmetryContext); narrative-bridge.js#buildNPCTraits injects 3 fields (persistent_grudge / current_preoccupation / desire_for_this_player); content/world/{grudge,desire,preoccupation}_templates.json; routes/worlds.js combat path records grudges on kin of killed NPC + dialogue endpoint seeds asymmetry; faction-strategy applyMove cascades into refreshFactionPreoccupations; tests (15/15). |
| Phase 1.5 — Knowledge Trade (May 2026, commit a391cbf) | Migration 127 (mentorships + npc_skill_acquisitions + skill_demonstration_log); `server/lib/npc-marketplace.js` (listNpcRecipesPass + intraNpcPurchasePass with ARCHETYPE_BUY_PREFERENCE; price = 50 × 1.10^revision_num); `server/lib/mentorship.js` (requestMentorship capped at mentor_depth-1; completeMentorshipSession; recordDemonstration; consumeDemonstrationsForNpc); `emergent/npc-marketplace-cycle.js` heartbeat freq 240 (~60min); `domains/knowledge-trade.js` (5 macros); npc-skill-author.js consumes demonstrations to bias next NPC revision toward player lineage; routes/worlds.js combat path records demonstration for target + nearby friendlies; tests (17/17). |
| Phase 1 — Skill Evolution (May 2026, ~2,216 LOC, 25 tests) | Migration 126 (skill_revisions + skill_evolution_unlocks); skill-evolution.js (composeDeterministicEvolution + applyEvolution + maybeLLMEvolve env-gated by CONCORD_SKILL_EVOLUTION_LLM); npc-skill-author.js auto-evolution on heartbeat; npc-skill-evolve-cycle freq 80 (~20min); domains/skill-evolution.js. |
| Per-Lens Polish + Cross-Lens Seamlessness sprint (May 2026) | **Substrate primitives** (every file confirmed present): `components/lens/LensShell.tsx` (130 LOC, headless context provider — applies a11y data-attrs to a wrapper, registers active-lens with the UI store, never imposes visual chrome); `components/lens/ManifestActionBar.tsx` (154 LOC, codemod-mounted in 158 thin lenses); `components/lens/EmptyStateCTA.tsx` (147 LOC, manifest-derived "Create your first {artifact}"); `components/lens/RivalShapePreview.tsx` (271 LOC, mounts the right rival silhouette per lensId); `hooks/useLensCommand.ts` (129 LOC, lens-namespaced keyboard registration). **DTU primitives**: `components/dtu/DTUEmbed.tsx` (263), `CitePicker.tsx` (235), `CitationConsentModal.tsx` (258), `CreatorBadge.tsx` (148), `components/federation/FederationBadge.tsx` (130). **Rival-shape silhouettes** (each one a real file on disk): `code/VSCodeShell.tsx` (239) · `crypto/WalletShell.tsx` (224) · `legal/DocsShell.tsx` (232) · `message/InboxShell.tsx` (188) · `whiteboard/WhiteboardCanvas.tsx` (248) · `healthcare/EHRShell.tsx` (191) · `marketplace/BandcampGrid.tsx` (209) · `accounting/KPIStrip.tsx` (171) · `atlas/GraphView.tsx` (261) · `music/SessionView.tsx` (283). New `app/lenses/message/page.tsx` (90 LOC) — the message lens didn't exist before. **Console-browser play infra**: `hooks/useGamepad.ts` (278 LOC, Standard Gamepad API + controller-flavor detection) + `hooks/useConsolePing.ts` (41 LOC). **Phase 16 backend**: `server/lib/goddess-arcs.js` (176 LOC), `server/lib/commune-templates.js` (131 LOC), `server/lib/console-stats.js` (146 LOC). **Tier-2 frontend tests** (13 new test files, 62 cases — verified `npx vitest run` all 13 files passing): `useLensCommand`, `ManifestActionBar`, `EmptyStateCTA`, `FederationBadge`, `DTUEmbed`, `CitePicker`, `CitationConsentModal`, `GraphView`, `BandcampGrid`, `KPIStrip`, `SessionView`, `InboxShell`, `RivalShells`. **Tier-3 server tests**: `tests/e2e/authoring-round-trip.test.js` (290 LOC, 8/8 passing — verified `node --test` against real `economy_ledger` schema covers single-derivative gen-1 royalty, 3-deep cascade halving 105/52.5/26.25, gen-50 floor at 0.0005, no-ancestor pass-through, multi-purchase non-double-pay, sourceTxId idempotency, private-parent consent block, ancestor-chain queryability) and `tests/goddess-arc-federation.test.js` (234 LOC, 18/18 passing — author validation, registry round-trip, phase resolution, JSON-wire export → import preserves compound-refusal override). **Documentation refresh**: `docs/AUDIT_INVENTORY.md` (854 LOC) — verified-only inventory generated by direct grep against the tree, single source of truth for lens/domain/migration/route/macro/heartbeat/table counts. CLAUDE.md cross-references it everywhere a numeric claim used to live. |
| v1 closeout sprint (May 2026) | **Phase A** — server lint at 0 errors / 0 warnings via per-glob legacy overrides + `eslint --fix` sweep; CI gate at `--max-warnings=0`; pre-commit hook auto-fixes staged server files; weekly scheduled `lint-sweep.yml` workflow opens reviewable cleanup PRs. **Phase B** — Forge polyglot template engine wired end-to-end (route `/api/forge/*`, 4 parity macros `forge.{list,sections,validate,generate}`, lens `/lenses/forge` mounting ForgeWorkbench, manifest entry lensNumber 124, 5/5 tests). **Phase C** — absorbed-libs audit: 3 wired (dtu-protocol + concord-test + concord-moderate, all converted CJS→ESM), 6 dropped (-9466 LOC of mock/duplicate code: identity, lens, procgen, sync, brain-service, component-registry), 2 queued for eval (concord-observe, cpm-manager). New `dtu.protocol_validate` macro for external-DTU envelope validation. **Layer 13** — NPC-initiated ambient conversations: migration 118 (`npc_conversations` sorted-pair PK), `lib/embodied/npc-dialogue.js`, heartbeat #18 `npc-conversation-initiator @ 8` emits `npc:conversation-bid` socket event, 12/12 tests, opt-in LLM via `CONCORD_NPC_DIALOGUE_LLM=true`. **Phase D** — UX Suite lens at `/lenses/ux-suite` mounts the 20 absorbed UX components in tabbed surface (Settings/Progress/World/Ops/Shell groups), 1 dropped (LocalizationProvider duplicate). **Pre-phase** — novel-files-extract surgical absorption of 46 files / ~19.8k LOC from 5 unmerged sibling branches (Forge, DTU protocol, world-lens UX, instrumentation). |
| Layer 12 (May 2026) | Wire-the-unwired: `server/emergent/lattice-orchestrator.js` registers 3 heartbeats activating 4 already-built engines (drift-monitor, breakthrough-clusters, cnet-federation, hlr-engine). Bridges drift HIGH/CRITICAL findings → runHLR(constraint_check). `initLatticeOrchestrator(STATE)` wired right after circuit breakers in server.js. 8/8 contract tests pin heartbeat-compatible signature + never-throw + init-required precondition. The corpus already contained 3635 LOC of contradiction-detection / cross-domain synthesis / federation / multi-mode-reasoning infrastructure — Layer 12 simply put it on a clock. |
| Layer 11 (May 2026) | Migration 117 (`faction_strategy_state` + `faction_relations` (sorted pair PRIMARY KEY) + `faction_strategy_log`); `server/lib/embodied/faction-strategy.js` (ensureFactionState idempotent seed, getRelation/setRelation auto-sorting pair, pickMove deterministic state machine with seeded RNG covering 6 stances and 8 move types, applyMove transactional log + state + cooldown + rival momentum mirroring + relations upsert, getRecentMoves); `server/emergent/faction-strategy-cycle.js#runFactionStrategyCycle` heartbeat (frequency 200, ~50 min). MOVE_COOLDOWN_S = 6h default. Tests at `tests/embodied-faction-strategy.test.js` (19/19) |
| Layer 10 (May 2026) | Migration 116 (`forward_predictions` — per-user speculative predictions about quests/npcs/factions/decisions/self with TTL + realisation tracking); `server/lib/embodied/forward-sim.js` (tryPredictForUser throttled by MIN_PASS_INTERVAL_S=4h, skips subjects with active predictions, 3 max per pass; composeDeterministicPrediction with per-kind seeded confidence — quest 0.62, npc 0.55, faction 0.42, decision 0.40, self 0.35; getActivePredictions, realisePrediction, sweepExpiredPredictions GC); `server/emergent/forward-sim-cycle.js#runForwardSimCycle` heartbeat (frequency 100, ~25min) — offline-only candidate set + cooldown-aware. LLM opt-in via CONCORD_FORWARD_SIM_LLM=true (subconscious brain, parses confidence from `|conf=0.NN` suffix). 15/15 tests; combined 87/87 across Layer 7.5/8/9/10 + combat-anti-cheat (15+25+18+14+15) |
| Layer 9 (May 2026) | Migration 115 (`dreams` — per-player record of offline dream-composition passes); `server/lib/embodied/dream-engine.js` (gatherFragments pulls last 12h from damage_events bidirectional, pain_signals, player_inventory, world_visits, creator-keyed dtus excluding kind='dream'; signature is sha256 of sorted fingerprint per fragment so re-runs in same window dedupe; composeDeterministic stitches grounded prose without LLM; tryComposeForUser throttles by 6h interval + signature dedupe + single-tx insert; getRecentDreams HUD helper); `server/emergent/embodied-dream-cycle.js#runEmbodiedDreamCycle` heartbeat (frequency 80, ~20min) discovers offline candidates and composes per user. Distinct from system-level `dream-cycle.js#runDreamCycle(STATE)` 6-phase substrate cycle. LLM enhancement opt-in via CONCORD_DREAM_LLM=true (subconscious brain, port 11435, 8s timeout, deterministic fallback). 14/14 contract tests + combined 92/92 across Layer 7/7.5/8/9 + combat-anti-cheat |
| Layer 8 (May 2026) | Migration 114 (`pain_signals` — per-player somatic ledger keyed by user×region×source); `server/lib/embodied/pain.js` (recordPain, regionForElement element→body table, getPainBudget aggregation by region, consumePainBudget idempotent ledger transition, decayProcessedPain GC); `server/emergent/repair-cycle.js` heartbeat (frequency 20, ~5min cadence) — consumes pending rows for each user, awards endurance/strength/agility/vitality/focus XP via `gainSkillXP` (REGION_XP_PER_PAIN_UNIT = 35), inserts `damage_resist` buff in `user_active_effects` (magnitude = totalIntensity × 0.04, capped at 0.25, duration 30 min); NPC-attack route wired to record pain on hit (intensity = clamp(finalDamage/100, 0.05, 1.0)); Tier-2 contract tests pin all helpers + full cycle integration (`tests/embodied-pain-repair.test.js` 18/18); 60/60 across Layer 7/7.5/8 + combat-anti-cheat |
| Layer 7 + 7.5 (May 2026) | Migration 112 (`embodied_signal_log` — per-world, per-50m-cell, per-channel signals with TTL decay; later unified by 113); `server/lib/embodied/signals.js` (recordSignal, signalsForWorld with 3×3 cell window + recency-weighted folding, decaySweep, seedWorldClimate); `server/emergent/environment-sensor.js` heartbeat (frequency 5; reads `worlds.rule_modulators.climate` + time-of-day; writes one baseline row per channel per active world); `server/lib/embodied/skill-environment.js` (elementalEnvBoost — frost/fire/water/lightning/bio/poison/energy potency multipliers; elementalEnvFeedback — bidirectional signal writes back to environment per element; terrainResourceBoost — bender wedges for harvest yield; shouldStaggerOnTerrain — DBZ-style projected-collision stagger; applyStructuralStress — Geo-Mod-light building state transitions standing→damaged→collapsed); combat attack route wired post-cap with env multiplier + feedback signal writes + terrain stagger + structural stress + realtime emits (`combat:stagger`, `world:building-state`); gather route wired with element-aware terrain affinity yield boost; new Tier-2 contract test file pinning all six helpers (cellOf, signalsForWorld round-trip + recency + locality + degrade-graceful, all element wedges, terrain bender table, stagger threshold + projection + collapsed-exclusion, structural-stress state transitions, env-sensor heartbeat baseline + override + decay) |
| Pre-deploy follow-on (May 2026) | Migration 101 (`player_inventory.world_id` — items now follow the player per world; tool-tree `craftTool` + `routes/player-inventory` scoped by `(user_id, world_id)`); server-side combat anti-cheat in `/api/worlds/:worldId/combat/attack` (`_validateCombatReach` reads live `cityPresence` against skill `range_m` capped at 80m, `_validateDamageCap` uses `skill.max_damage * 2.5` or 500 hard cap); world-event `endEvent` now mints **real CC** to attendees via `mintCoins` with `event_reward:${eventId}:${userId}` refId for idempotency (pre-fix the toast lied — the wallet stayed flat); heartbeat-skipped Prom counter `concord_heartbeat_skipped_total` + `ConcordHeartbeatOverrun` alert; NPC-secret leakage contract test (`buildNPCTraits` exported, structural omission + canary-scan defense-in-depth); content-seeder validators (`validateNpc`, `validateFaction`, `validateQuest`, `validateLoreEvent` — malformed records logged + skipped instead of corrupting state); migration 098 hub-id-reconciliation regression test; backup-restore round-trip integration test; LLM router + vision contract tests gated on `CONCORD_BEHAVIOR_TEST_LLM=true`; royalty cascade real-DB test against the actual `economy_ledger` schema; WebSocket `event-shapes.js` registry with `validateEvent` dev-mode-only validator; **emergent simulation UI** — new `EmergentEventFeed` floating panel surfaces 20 silent simulation events (entity:death / body:* / agent:insights / forgetting:cycle_complete / dream:captured / lattice:meta:* / attention:allocation / evo:asset-promoted / world:refusal-field / world:crisis* / weather:update / world:event:scheduled / faction-war:* / dtu:promoted / pain:wound_*) with channel filters + pause; world-page mounts it next to `DistrictActivityFeed`; SocketEvent union extended with `world:crisis` / `world:crisis-resolved`; +1 test file for combat anti-cheat (15/15 passing); test totals at landing: **9508 main + 1212 behavior = 10,720 / 0 fail** (post-Sprint-D the same tree runs 10,940 main + 1304 behavior = 12,244 passing — see "Test suite" section for current totals) |
| Production Audit (May 2026) | Migration 100 (`evo_assets` CHECK extended for gameplay kinds); 4 load-bearing bugs **made actually load-bearing** — refusal-field glyph algebra (was decorative-by-bug, now strength≥6 truly gates compound-refusal phases), world-event scheduler (silently created 0 events due to wrong field names to `createEvent`), gameplay-asset-bridge (every event silently dropped — schema CHECK + missing `localPath`), lens manifest (10 duplicate `lensNumber` collisions, renumbered to 113–123 contiguous); heartbeat counter alert path comment fixed; quest-engine `require()` → `await import()` (now consistent across server.js — five await-import sites, see grep `await import.*quest-engine` for current locations); `agent_threads.accumulated_state_json` and `agent_thread_checkpoints.node_id` NOT NULL bugs in thread-manager; promoted `better-sqlite3` from optional to required; full route-inventory updates to API.md (onboarding, tutorial, creator-economy, openapi/docs, concord-link, black-market, world bazaar/perf-telemetry); Three-Gate sync (17 missing routes added to Gate 3); +6 new test files (Tier-2 contract: dtu-quality-scoring, refusal-algebra/strength-gating, world-event-scheduler, gameplay-asset-bridge, npc-schedules; Tier-3 E2E: first-cycle-journey); 56 pre-existing assertion failures resolved (brain-routing 5-brain inventory, lens-features 58/220→58/274 update, accumulator multi-tenant caps, citation consent, storage/session limits, oauth, openapi coverage threshold, social-pings rate test, economy 48h withdrawal hold, physics validation rescale loop, routes/media auth shape) |
| Concordant Web | 8 cross-world authored major characters + 3 factions + Concordant Law + Sovereign Refusal Archive + Mass Raid scaffold + Refusal Field new kinds (numbers/dome/win refused) |
| EvoEcosystem | Migrations 094–096; fauna spawner; loot tables; butcher route; cooking pipeline (raw→cooked→buff); active-effects table; Concordia neutral-zone middleware; Refusal Field; Three Pillars seed |
| v2.0 Bidirectional Creative OS | Heartbeat registry; recipe substrate; social-NPC bridge; music/blueprint/medical instantiation; multi-avatar; federation; realtime; DAW layering |
| Audit pass | Heap → 32GB; lifted 9 artificial caps; fixed 6 broken items + 14 polish items; +5 integration / migration tests |

---

