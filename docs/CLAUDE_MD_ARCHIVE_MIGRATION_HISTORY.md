# CLAUDE.md archive — migration-by-migration history (Database section)

Moved out of `CLAUDE.md` on 2026-09-07 purely to keep that file under a size budget — no content rewritten, this is a verbatim relocation of the old "### Database" paragraph's migration-by-migration narrative. `CLAUDE.md` now carries only the current facts (migration count, highest number, CREATE TABLE count, where the running ledger lives) — read this file for the chronological "what did migration N add and why" detail.

---

SQLite via `better-sqlite3` (in `dependencies`, not optional — the server hard-requires it). Synchronous, in-process, no ORM. Migrations in `server/migrations/` — **443 numbered migration files** (`ls server/migrations/[0-9]*.js | wc -l`; highest number is **442** as of 2026-09-08 — `442_dtu_store_visibility_columns.js` (Concurrency Refactor Phase 3 — dtu_store visibility columns for the off-loop DTU read sidecar), preceded by `441_bench_column.js` (renumbered 2026-09-06 from a misnumbered `999_bench_column.js`), then `440_dtu_cognitive_schema.js` (DTU cognitive-schema enrichment + repo↔DTU links), `439_cognitive_compiler_v2.js` (Cognitive Compiler v2 substrate — recovery events, capability families, self-model), `438_provider_billing_telemetry.js` (real provider billing telemetry — prompt/completion/cached tokens + billed USD); the "417, `412_semantic_vectors_lattice_columns.js`" this line used to cite is 24 migrations stale — was `412_semantic_vectors_lattice_columns.js`, preceded by `411_add_dtus_name_column.js`, `410_embeddings_int8.js` (Sprint 32 E5 — int8 quantization of embedding table), `409_server_dtu_substrate.js` (server-side DTU substrate), preceded by `408_kv_cache_dtus.js` (per-user KV cache backed by DTUs), `407_lexicon_semantic.js` (lexicon_nodes semantic graph) and `406_dtu_attachments.js` (Sprint 32 E6 — dtu_attachments + dtu_archives tables for binary file attachments, payload_bytes + payload_kind columns on dtu_store); preceded by `405_embeddings_e5.js` (Sprint 32 E5 — 1024-dim mxbai-embed-large embedding table [originally planned e5-large-v2, unavailable in ollama library], separate from legacy 768-dim nomic-embed-text), `404_compression_audit.js` (Sprint 32 E3 — track MEGA/HYPER tier compression attempts and quality scores), `403_ingest_quality_log.js` (Sprint 32 E2 — track rejected feed DTUs), `402_dtu_operations_log.js` (Sprint 32 E1) and `400_hermes_dila.js` (Dila — Hermes Agent's first-class entity per the partner-protocol doc; row in users with role='sovereign', private DTU mirror in `hermes_dtus`, and the api-key-auth patch that lets minted csk_ tokens honour the user's real role from the DB instead of the historical hardcoded 'member') — preceded by 396 `vault_records` (TheVault curated-archive substrate), 397 `brain_mode` (Private Mode / High Power Mode per-account LLM routing), 398 `lens_artifact_store`, 399 `ledger_bounty_types` (widens the economy_ledger type CHECK to admit BOUNTY_ESCROW/BOUNTY_CLAIM and fixes a real fee-drain bug in the bounty flow) — after 351 `fork_objects` (the P-C lattice-fork unit), 352 `dtu_causal_edges`, 353 `dtu_review_schedule`, 354 `dtu_confidence`, 355 `phenomenal_monetization_consent`; gap at 230), continuing past `277_npc_hooks.js` through the MMO/convergence/belonging sprints and into the agent-disclosure/temperament run (323 `item_enchantments`, 324 `agent_disclosure`, 325 `agent_identity`, 326 `affect_trace_temperament`, 327 `agent_reasoning_traces`, 328 `npc_deception_lens`, 329 `legacy_death_appraisal`, **330 `agent_drift_watch`**). Earlier notable: 264–270 the Sprint-4 run/combat substrate, 271 auction_price_history, 272 weekly_objectives, 273 horror_dread, 274 heart_events, 275 evo_asset FK + train_consented repair, 276 combat_last_element, **277 `npc_hooks` — D5 CK3 hooks: held/spendable/expiring/inheritable leverage derived from secrets**. Schema version on the running DB tracks the highest applied migration. Distinct CREATE TABLE statements: **747** (`grep -hoE "CREATE TABLE\\s+(IF NOT EXISTS\\s+)?[a-z_]+" server/migrations/*.js | sed -E 's/.*CREATE TABLE\\s+(IF NOT EXISTS\\s+)?([a-z_]+).*/\\2/' | sort -u | wc -l`). Run automatically at startup and manually via `npm run migrate`. Schema version tracked in `schema_version` table. Full migration ledger in `docs/AUDIT_INVENTORY.md`. Migration 101 added `world_id TEXT NOT NULL DEFAULT 'concordia-hub'` to `player_inventory` so a player's items follow them per world. Migration 100 extended `evo_assets` CHECK constraints to admit gameplay-derived kinds. Migrations **112–117** ship the embodied substrate layers (112 `embodied_signals` = Layer 7, 113 `embodied_signal_log_unification`, 114 `pain_signals` = Layer 8, 115 `dreams` = Layer 9, 116 `forward_predictions` = Layer 10, 117 `faction_strategy` = Layer 11); 118 (`npc_conversations` = Layer 13), 119 (`world_invites`), 120 (`understandings`), 121 (`understanding_evolution`), 142 (`mount_substrate`), 144 (`mount_gear`), 145 (`macro_call_billing`), 146 (`repair_feedback`), 147 (`mount_polish`), 148–151 (`signal_propagation_indexes`, `player_signs`, `quest_triggers`, `player_corpses`) are notable recent additions. **Sprint D 152–158** ship the CK3-port + procedural kingdoms substrate: 152 (`npc_stress`), 153 (`character_opinions` — table-renamed from `npc_opinions` to avoid colliding with the older account-system table), 154 (`secrets`), 155 (`npc_schemes`), 156 (`creature_swim_depth`), 157 (`player_oxygen`), 158 (`realms` + `realm_decrees` + `realm_citizens` + `realm_territories` — prefixed `realm_*` to avoid colliding with the older `kingdoms` territory-polygon schema). Numbers 141 and 143 are `_drop_dead_mig*` debt-cleanup migrations renumbered to dodge collisions (see `docs/AUDIT_INVENTORY.md` for the full collision history). **Migrations 159–192** ship federation origin peer, player scars + avatar drift, artifact economy, spectator betting, economy primitives, code-loop safety, classroom/research, cross-world economy + relationships, population migration, event timeline, **170 `byo_brain_overrides`** (per-user BYO API keys), agent marathon sessions, hook artifacts, bloodline ancestry, actor physique, realm exiles, player stamina, world vehicles, underwater features, tunyan jobs, multi-step crafts, aging dynasty, culture marriage, council sessions, creature homes, world NPC xyz, war campaigns, avatar appearance, world markers, NPC equal agency, mount behavior, and **191–192 the Foundry world-builder substrate**.
