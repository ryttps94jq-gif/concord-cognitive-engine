> See [`docs/STACK_REALITY.md`](docs/STACK_REALITY.md) for measured LIVE vs OVERCLAIM (2026-09-05).

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 🟢 ACTIVE CONTINUATION — read `docs/NEXT_ARC_PLAN.md` first

**DHTP Representation Sufficiency (cloud agents):** If the task is the DHTP-RS benchmark, read **`docs/DHTP_RS_CLOUD_AGENT.md`** first. Entry point: `node server/scripts/run-dhtp-rs.mjs resume` (auto-loads keys from `.env.runpod` / `server/.env`). Status: `node server/scripts/run-dhtp-rs.mjs status`. Phase 1 complete; **Phase 2 pending** (6 probes × 20 trials).

The v1 release arc is merged (PR #841, 2026-07-02). The live arc is now
**`docs/NEXT_ARC_PLAN.md`** (the "🟢 HANDOFF — start here" block at the top): Wave 1 =
**ConKay JARVIS flagship** (spatial FUI cockpit, honest stage-beats, artifact→interactive-3D)
+ **shared primitives** (provenance-stamped ingest, X-as-DTU listing, lattice-fork object,
governance design doc), with the audited 43-idea backlog behind it. The ConKay design detail
still lives in `docs/CONKAY_HONEST_HOLOGRAM_PLAN.md` (Track-B design source; note its Phase-2
`@react-three/postprocessing` blocker is STALE — the dependency is installed now).

- **Honest depth floor:** **0.688** (`node scripts/grade-macro-depth.mjs --honest`), climbing toward the ~0.73 ceiling. Read this number precisely: **100% of macros carry shape/smoke coverage** (the CI harness derives a case per macro) and **~70% of the non-`utility` macros stand on a real behavioral test**; 0.688 is a *weighted* score that taxes correctly-small utility code at 0.6 by design — it is NOT "31% untested."
- **Strategic companion:** `docs/SCIFI_FEASIBILITY_MAP.md` — code-grounded audit (2026-06-08). It corrected two load-bearing facts: engineering **CAS + beam-frame FEA are a real STRENGTH**, and external **connectors were scaffold** (only MCP + OAuth-signin were real → was **Track C**). **Update 2026-06-09: Track C done — Gmail + Google Calendar are now real two-way** (send/push + read/inbox/pull on the SSRF-guarded `connectorFetch` chokepoint, encrypted per-user tokens, polished GmailSection inbox client; live use needs only a Google OAuth client — `docs/CONNECTORS_GO_LIVE.md`). **Update: Slack/Sheets/GitHub/Notion are now built too** — real `server/domains/{slack,sheets,github,notion}.js` + `connector-client.js` readers on the same SSRF-guarded `connectorFetch` chokepoint, contract-tested with injected fetch (`connector-read-paths` / `connector-github-commit-status` / `connector-extra-paths` / `connector-oauth-flow`). All six marquee connectors are code-complete; going live needs only operator-supplied OAuth client credentials — the same operational (not code) gate as Gmail/Calendar. The "next thin adds" framing below is stale.
- **Three tracks (owner-locked, pre-Wave-1 framing):** **(A) Depth sweep** — fleet loop toward the ceiling (`node scripts/depth-backlog.mjs` → subagents → guard → commit → push). **(B) ConKay honest hologram** — superseded: this track's "Phase 2" is now Wave 1's **Track K** in `docs/NEXT_ARC_PLAN.md`, and as of 2026-07-03 Track K (K1–K6, K6-voice) **and** Track P (P-A/P-B/P-C/P-D) are both fully **SHIPPED** — see that doc's §B/§C for per-phase commit hashes. Next work pulls from the ranked backlog in §D, not from a "Phase 2." **(C) Marquee connector honesty** — ✅ **done: all six marquee connectors are code-complete.** Gmail + Google Calendar real two-way (2026-06-09); Slack/Sheets/GitHub/Notion now built + contract-tested on the connector-agnostic `connectorFetch` core (`server/domains/{slack,sheets,github,notion}.js` + `connector-client.js` readers + injected-fetch contract tests). Live use is gated only on operator OAuth client credentials, not code.
- **Non-negotiable for Track B:** *honest by construction* — every animated element is a pure function of a real backend event; **no `setInterval`/fake-progress.** Audit: `grep -rE "setInterval|setTimeout" concord-frontend/components/conkay/`.
- The strategic framing (verified + private compute-agent for R&D; verification IS the product) and the full design + research live in that doc. The `/root/.claude/plans/` copy is container-ephemeral — the repo doc is the source of truth.
- **MMO/RPG completeness (2026-06-26):** `docs/MMO_RPG_COMPLETENESS_AUDIT.md` — a 21-pillar "is the game complete & playable" framework + a code-verified Concordia scorecard + a fixed-defect ledger + a re-verified backlog. **Backlog re-checked 2026-06-26 and largely CLOSED:** gear durability/repair SHIPPED; the ~105-site schema-drift batch is CLOSED (`node scripts/verify-schema-drift.mjs --ci 0` → DRIFT 0, CI-gated against recurrence); the POLISH_AUDIT combat-feel seams are FIXED (pinned by `feel-consolidation.test.ts`). The only residual is optional/non-defect (recorded-audio-vs-synthesis taste call, balance-constant tuning, invariant-engine override authoring). Backed by `docs/research/MMO_RPG_GENRE_RESEARCH.md` (cited genre research) and `docs/research/WIRING_INTEGRITY_AUDIT.md` (caller↔receiver audit). This pass fixed the unknown-macro LLM-mask (now fail-fast `unknown_macro`), the `dtu.create` phantom-success data loss, the `player:low-health` phantom listener, the hardcoded minimap position, the malformed maintenance-gates critical, CharacterCustomizer's fabricated wardrobe (now a real `appearance.options` catalog), the `/dialogue/respond` flat-stub fallback, and the 9 orphan socket emits + 12 dead `concordia:*` CustomEvents; and added Character Sheet / Ability Cooldown HUD / Target Nameplate to the world lens.

---

## What This Is

Concord Cognitive Engine is a cognitive operating system — a knowledge platform with **266 frontend lens directories**, **434 backend domain files**, four parallel LLM brains + LLaVA vision, a self-compressing knowledge substrate (DTUs), a creator economy with perpetual royalties, a seven-layer mesh network, and a 3D civilization simulator (Concordia). Live at concord-os.org. **For a verified current snapshot (numbers reproduced from commands, this-arc shipped work, novelty inventory, audit results) see `docs/STATE_OF_CONCORD.md`.** **Before building anything new, check `docs/NOVELTY_INVENTORY.md` (~326 distinct novelties / 34 groups, each with its source file) — it's the build-reference map for "does X already exist / where does it live," so you don't rebuild or break an existing subsystem.** Run `npm run count-loc` for the current authored-source LOC when a figure is actually needed (excludes node_modules/build/generated; reclassifies data-modules masquerading as code — e.g. the deprecated 145k-line `server/dtus.js` seed pack at 0% code density — out of the source total). Cite the counter's output, never a remembered number. (LOC is an internal engineering signal, not a headline — don't lead with it.)

**Wiring status** — reproducible via `node scripts/verify-lens-backends.mjs` (**re-run 2026-09-07: `{WIRED:262, NO-BACKEND-CALL:4, total:266, macroDomains:558, routePrefixes:3042}`** — the prior "231 WIRED / 3 broken" below is superseded; the 3 broken wires were closed and the verifier now reports 0 broken / 0 partial. The 2026-08-01 snapshot this used to cite — `{WIRED:263, NO-BACKEND-CALL:2, total:265, macroDomains:547, routePrefixes:2983}` — is stale; total lens count grew by one and two more lenses (`ar`, `art`) now read NO-BACKEND-CALL, see below):
- 262 lenses **WIRED** (98.5%) — page or imported child component calls a registered macro / known REST route / generic lens hook.
- **What "WIRED" measures:** the verifier checks that a lens *reaches* a registered macro or REST route — a reachability/coverage check, run statically. Behavioral testing is the separate signal for whether the reached code does the right thing: the depth-fleet sweep behaviorally tested ~21 waves of macros + a de-demo pass + 15 new backend domains and surfaced only ~9 real source bugs total — a low defect rate, so wired lenses mostly work front-to-back. The genuine remaining weaknesses are in presentation polish + connector depth, not in the macro layer. For a specific lens's behavior, read its macro + its depth/behavior test.
- **0 PARTIAL** post-Phase-Z (previously 3): the `feed` + `social` lenses now resolve through a Z4 POST alias on `/api/connective-tissue/search`. **The `personas` line below is superseded (2026-07-10 Wave 3 audit) — `server.js`'s `register("personas", ...)` shadow loop + 5 Z4 stubs still exist (marked `note: "intentional_shadow_ok"`) but a full 17-macro Character.AI-parity domain (`server/domains/personas.js`: create/update/get/mine/delete/publish/browse/facets/chat_open/chat_send/chat_history/rate/stats/install/revise/versions/regenerate_portrait) now registers via `registerLensAction` into `LENS_ACTIONS`, which `/api/lens/run` prefers over the `MACROS`-registered shadow loop (`server.js:39592`) — so the real domain wins for all frontend traffic, and the Z4 stubs' `{ok:false, reason:'roadmap'}` "coming soon" behavior no longer fires. Publish/install/versions/rate/chat are all real and wired; see `docs/lens-specs/personas-capability-map.md`.**
- **4 NO-BACKEND-CALL** (per `scripts/verify-lens-backends.mjs`), only 2 of which are actually by design: `ux-suite` is a navigation directory page that links to where each absorbed UX component genuinely mounts; `narrative-walk` is a self-contained authored-narrative reader — neither has an API surface of its own. **The other 2 (`ar`, `art`, first seen 2026-09-07) are a verifier false-negative, not a real gap** — both are `page.tsx` → `dynamic(() => import('./page-client'))` wrapper shells (Next.js code-split loader), and the verifier's static scan doesn't recurse through that indirection into `page-client.tsx`. Checked both directly: `art/page-client.tsx` has 2 real backend calls, `ar/page-client.tsx` calls the real macro-backed `useLensData('ar','Model3D',...)` hook. Same under-detection class as the historical "0 broken / 0 SCAFFOLD" correction two paragraphs below — the verifier still doesn't traverse dynamic-import wrappers. Don't "fix" these two lenses; fix the verifier's recursion, or just know to discount this pair.

The earlier "0 broken / 0 SCAFFOLD" claim in this file relied on a verifier that under-detected (3 regex bugs: no TS-generic match, no child-component recursion, no `api.get`/`apiClient` recognition). After fixing the verifier, the three real broken connections above showed up.

**Feature-parity claim is currently contradictory** — `docs/lens-specs/README.md` says "~91% average parity, 0 buildable features in backlog" (all spec files have every Missing item checked `[x]` shipped). Read claim language critically — "shipped front-to-back" sometimes covers a working stub. **But note (2026-05-29 depth audit, `docs/DEPTH_BALANCE_PLAN.md`): two long-standing "oversold stub" examples in this file were themselves stale and have been corrected** — `code/Live Share` has a *real Yjs CRDT path* (`server/lib/yjs-realtime.js`, `Y.Text` bindings, `attachYjsSync` @ `server.js:7810`) alongside the legacy `code.liveshare-poll` macro; `healthcare/telehealth-create` is a *real WebRTC video client* (`components/healthcare/TelehealthVideoCall.tsx` + `simple-peer` + `server/lib/webrtc-signalling.js`), with Daily.co only an optional fallback. The genuinely-oversold cluster the audit found is the **music lens** — but even this was partly stale: **`music.ai-playlist` IS wired** to the LLM with a deterministic keyword fallback (`server/domains/music.js:1195`, verified 2026-05-29 continuation audit), so the "AI-playlist unwired" claim was wrong. The collaborative-playlist *edit* path is now wired too (cross-user add/detail/list — `domains/music.js` `findAnyPlaylist`/`findTrackAnyUser`, `tests/music-collab-playlist.test.js`). **Free-API ingestion IS wired in code** (2026-07-04 re-audit: `server/domains/music.js:998` iTunes Search, `:1073` Jamendo with real streamable URLs, `:1103` Audius) — it needs outbound network egress from the deploy box at runtime, but the earlier "genuinely unwired" claim here was stale. **EQ + preamp are now genuinely wired** (2026-06-03 lens-audit fix): `lib/music/player.ts` builds a real Web Audio chain (source → 3-band BiquadFilter EQ → preamp gain → analyser → out) and `MusicParityPanel` calls `applyAudioSettings()`, so the sliders actually change the sound (audible output still needs device/browser QA — the node graph is correct by construction, not heard headless). **Karaoke vocal-cancel + true track-to-track crossfade are now IMPLEMENTED** (2026-07-04 re-audit: `concord-frontend/lib/music/player.ts:7-15` is a dual-deck graph enabling real crossfade, `:281-288` OOPS L−R center-channel vocal cancellation; the earlier "remain flagged" claim was stale — audible QA on a real device is still the honest residual). `download-add` is an honest offline *metadata queue* (`bytesStored:false`, `estimatedSizeKb`) — it was previously reporting a fabricated `sizeKb` as if audio had been stored. Trust the code over any doc, including this one. The earlier "~50% parity / ~1,686 buildable backlog" figure was from a snapshot before the May 2026 batch closeout work; it's stale.

**Macro depth distribution** — reproducible via `node scripts/grade-macro-depth.mjs` (output at `audit/macro-depth.json`). Each `(domain, macro)` pair is classified on four tiers based on combined-LOC + state-touch + try/catch + realtime/cross-macro/external-IO/heartbeat-delegate/artifact-write signals + test/frontend usage. The grader credits `hasTest=true` for every macro covered by `server/tests/behavior/lens-behavior-smoke.behavior.js` (the auto-derived shape-contract harness that runs every macro at CI time, modulo LLM-hint and destructive-hint name skips), since the harness derives cases from the live MACROS map at runtime and contains no static `"domain.macro"` references for the grader to grep. Helper-function bodies in the same source file are recursed one level for LOC and two levels for signal detection. Latest run (HEAD post-Phase-B, 2026-05-23):

| Tier | Count | % | Definition |
|---|---:|---:|---|
| stub | 0 | 0.0% | ≤15 LOC AND no state AND no external I/O AND nothing exercises it. |
| functional | 0 | 0.0% | substantive but missing one production-quality signal. |
| utility | 4,572 | 54.2% | correctly-small handlers (≤40 LOC) that are exercised, no external I/O. Catalog enums, pure formatters, tested delegations. Weight 1.0. |
| production-grade | 3,870 | 45.8% | qualifies via at least one of four paths: (A) ≥40 LOC + state + exercised + robustness; (B) externalIO + tryCatch + exercised; (C) ≥40 LOC pure-compute + tryCatch + exercised; (D) tested delegation. |

**Read both numbers (re-run 2026-06-02 on the current 8,698-macro tree, AFTER the grader was made honest in both directions — see below).** The grader had three bugs that the rework fixed: (1) it crashed on the grown frontend (`RangeError: Invalid string length` from `join`-ing 4,400 files — fixed by per-file reference collection); (2) it was DELEGATION-BLIND — a thin macro that delegates to an imported lib fn (e.g. `crime.record` → `recordCrime` in `crime-engine.js`, tested in `crime-engine.test.js`) was mis-graded as an untested "stub" because the grader only read the wrapper body, not the tested delegate; (3) it credited `hasTest` from a STATIC STRING MENTION alone — gameable. The rework makes credit **invocation-based** (a literal `runMacro("d","m",…)` or a delegate a test imports-and-calls) and **delegation-aware** (inherit the delegate's LOC + signals), and treats bulk CASES-loop/smoke coverage as **shape-only** (default mode only). Pinned by `server/tests/grade-macro-depth.test.js`.
- **Default (generous): 0.999.** Smoke-shape + bulk shape-loops + literal invocations all count; `utility` weighted 1.0. Distribution: stub 0 / functional 15 / utility 4,803 / production-grade 3,880.
- **Honest floor: `--honest` → 0.688** (2026-06-08; was 0.527 when this section was first written, climbed via the depth-fleet sweep — see below). The genuine BEHAVIORAL-coverage cut: only literal per-macro invocations + tested delegations count (NOT smoke, NOT bulk shape-loops); `utility` weighted 0.6. Reproduce: `node scripts/grade-macro-depth.mjs --honest`. (`STATE_RE` now recognizes a bare `db.prepare/run/exec/transaction` call, not just `ctx.db`/`STATE` — so the lib fns thin macros delegate to are correctly graded as state-touching; honest measurement, verified each reclassified macro really writes the DB.)

Note the honest floor DROPPED from the earlier 0.539 to **0.522** after the grader rework — that's the rework working: 0.539 was inflated by shape-only contract-loop credit `--honest` shouldn't grant; removing it (−304 production) outweighed the delegation fix recovering real-but-hidden coverage (−108 stubs). It then ticked up to **0.527** via (a) the `db.prepare` state-recognition fix above and (b) two batches of REAL behavioral tests (`server/tests/depth/*-behavior.test.js`: welding, plumbing, carpentry, masonry, hvac, manufacturing, observe, billing, audit — ~58 tests, **64 macros** moved functional/stub→utility/production on exact computed-value + round-trip + validation-rejection assertions via the `lensRun` harness). The grind is now quantified: ~30 macros per ~4-domain batch ≈ +0.002–0.003 to the floor, so reaching ~0.60 honest needs ~hundreds more behaviorally-tested macros — a sustained, per-domain effort following the `tests/depth/` template. **Honest 1.0 is structurally unreachable** without gaming: the `utility` weight is 0.6 by design (correctly-small handlers can't become "production-grade" without padding), so the real target is the ceiling (~0.75–0.85 if everything is tested), not 1.0; cite the `--no-honest` default (0.999) for "is it shipped/working". The "~18% behavioral" snapshot that used to sit here is **stale** — it was the floor at 0.527. Cite the band **0.69–1.0**, floor first. The "all production-grade or utility" framing below describes the generous rubric. **(Update 2026-06-08: the parallel-subagent "depth fleet" sweep has raised the honest floor to 0.688 — ~21+ waves across ~90 domains now carry behavioral tests; 100% of macros have shape/smoke coverage and ~70% of the non-`utility` macros stand on a real behavioral test. See `docs/DEPTH_FLEET_PLAN.md` for the live status, the self-maintaining backlog, the per-agent prompt template, and how to resume. The whole sweep — ~21 waves + a frontend de-demo pass + 15 new backend domains — surfaced only ~9 real source bugs *total* (`calendar.scheduleOptimize`, `sports.injuryRisk`, `defense.resourceAllocation`, `schema.schemaValidate`, `supplychain.scenarioSimulate`, `staking.open_stake` dead-validation, `game.economySimulate` ||-falsy, `marketplace.purchaseWithRoyalties` clone TDZ, `sim` feedback-loop polarity). That low defect rate over so many behaviorally-exercised macros is the load-bearing signal: the macro layer mostly works — weight it over the older cautionary framing.)**

Weighted depth score (generous rubric): **1.000** (stub=0.2, functional=0.6, utility=1.0, production=1.0). Path from 0.554 baseline:

| Stage | Weighted | Delta |
|---|---:|---:|
| Baseline (before calibration) | 0.554 | — |
| Phase A — grader calibration (smoke coverage credited, utility tier added, robustness signal broadened) | 0.898 | +34.4pp |
| Phase B — `scripts/auto-wrap-trycatch.mjs` AST-wrapped 1,360 functional handlers in try/catch + tier-rule refinement (4 production paths) | 0.986 | +8.8pp |
| Phase B+ — narrowed behavior-smoke skips (council + destructive opted back in; dispatcher catches throws) | 0.989 | +0.3pp |
| Phase C — `server/tests/llm-hint-macros-contract.test.js` adds shape-contract coverage for the 26 LLM-hint named macros the smoke harness still skips | 1.000 | +1.0pp |

Production-grade includes `dtu.create` (428 LOC), `code.codebase-chat`, `code.lsp-hover`, `code.liveshare-poll` (a legacy polling path; the real-time multiplayer surface is the Yjs CRDT in `server/lib/yjs-realtime.js`, so the spec's "multiplayer" claim is actually backed — see the 2026-05-29 audit correction above), `healthcare.telehealth-create`, `healthcare.fhir-export`, accounting formulas (`budgetVariance`, `trialBalance`, `profitLoss`), the royalty cascade entries, external-API integrations (`art.aic-search`, `astronomy.apod`), several hundred economy + agent + creative macros, and tested delegations. Utility includes catalog enums, small validators, and delegation wrappers — everything that's correctly small.

No tier definition was loosened beyond what spot-checks defended. No macros were deleted to inflate the denominator. The same 8,442 macro pairs from the baseline are still there — they just landed in honest tiers after the grader was fixed (Phase A) and the missing try/catch wrappers were added (Phase B) and the LLM-hint contract test was written (Phase C). Test impact: main suite 19,661+ pass / 0 fail, behavior suite 2,289+ pass / 0 fail.

**Source-of-truth inventory:** `docs/AUDIT_INVENTORY.md` is itself stale at HEAD `21c8f34` (it was last regenerated at HEAD `6d32663`, before migrations 193–201, the absorbed-libs work, and ~60 additional domain files landed). Numbers in this file are the post-2026-05-23 direct-grep counts; if the inventory disagrees, the inventory is the stale one. Reproduction commands sit next to every number below.

---

## How we work here — the method (read this before you build)

This project runs on **one principle: don't trust, check** — the same "verification IS the product" thesis, turned inward on the development process. Concord is only buildable at this scale by few people because verification does the trust that a large team normally does through review. The practices below are load-bearing; they are why a codebase this large can be touched without rotting. Follow them.

### 1. Runtime-truth over source-guessing (instrument, don't grep)
When you need to know what's *actually* happening, make the running system tell you — don't infer it from a static read. Static grep repeatedly produced **false** conclusions this project has been bitten by (a "dead" event that was consumed via a `subscribe(evt.name)` data array; an "orphan" emit whose doc-suggested removal would have deleted a live gameplay HUD). The runtime is the authority:
- **Open handles / "why won't the process exit":** `process.getActiveResourcesInfo()` + an `async_hooks` init hook capturing `new Error().stack` on `TCPWRAP`/`Timeout` construction names the exact leak site in one boot (this is how the `--test-force-exit` handles were found: worker MessagePorts + undici keep-alive pool sockets + raw module-scope timers — see `server/tests/preload/no-egress.mjs`).
- **Event wiring liveness:** run `server/lib/detectors/dead-event-listener-detector.js`, not a raw-string grep — the abstraction (shared `const` event names, subscribe-over-array) defeats grep.
- **The `_tickRssDomain` indirection lesson (2026-07-25):** a literal-string scan of `server/` for a socket event name can misreport live events as dead, because `server/emergent/realtime-feeds.js#_tickRssDomain(domain, feeds, eventName, bridgeType, realtimeEmit, opts)` takes the event name as a **parameter**, not a literal — the current literal scan count is **11** (`grep -r '"legal:update"'`), while the 11 callers (`tickLegalFeeds`, `tickGovernmentFeeds`, `tickRealEstateFeeds`, `tickAviationFeeds`, `tickInsuranceFeeds`, `tickManufacturingFeeds`, `tickLogisticsFeeds`, `tickRetailFeeds`, `tickFitnessFeeds`, `tickAgricultureFeeds`, `tickEducationFeeds`) each pass their event name (e.g. `"legal:update"`) as an argument, so a narrow literal scan can miss the actual emit inside the shared helper. Two separate passes got this count wrong today before it was caught by actually reading the helper body instead of trusting the grep. Same failure mode as this section's other two examples — an abstraction (a shared helper parameterized over the event name) defeats a literal scan; when a name-liveness check disagrees with your intuition about a well-factored file, read the helper before trusting the grep.
- Rule: if a claim is checkable at runtime, check it at runtime before you write it down or act on it.

### 2. Compute-don't-guess — use Concord's own engines as an oracle (Concord builds Concord)
Concord's ~9,600 macros include deterministic engines an LLM is *unreliable* at reproducing (CAS `domains/math.js`, beam-frame FEA `lib/simulation/fea-solver.js`, glyph algebra, craft-resolve, royalty cascade, economy conservation, combat-impact). **Boot the server and call them instead of doing the math in your head.** The harness makes this one function call:
```js
import { lensRun } from "./server/tests/depth/_harness.js"; // boots server.js once, in-memory
const r = await lensRun("astronomy", "celestialPosition", { data: { name: "Rigel", rightAscension: 5.5, declination: -8.2 } });
// r.result.ra === "5.242h" — the engine is the spec, not your arithmetic
```
Use this to **generate/verify test expected-values** (the depth-test methodology's "verify the formula, don't paste output" — the engine is how you do it right), to check economy conservation, to validate any computed claim. When authoring a depth test, the correct expected value comes from the engine, hand-checked for sanity — never from an LLM's own arithmetic. The full deterministic set works in-sandbox (no brains needed); LLM-gated macros need Ollama and degrade honestly.

### 3. Honest by construction — fabrication must leave fingerprints
No fake/mock/simulated data in a shipped path. A surface that can't do the real thing returns an **honest failure** (`{ ok:false, reason:'no_token' }`, a "pending"/loading state, an explicit "not yet wired" note) — never a fabricated success. When wiring a UI to a substrate, wire **only** the fields a real backend produces; sections with no substrate stay unrendered with a documented reason. This rule holds for subagents working unsupervised too: if you can't source it truthfully, don't render it, and write down why.

**Zero demo content is a hard invariant, not a stated goal (2026-07-09).** The Frontend Rebuild Program's per-lens audits keep finding the same defect class in older frontend pages — real backend depth sitting disconnected behind fabricated data presented as if live: a client-side-invented "Match: 87%" badge next to an unused real matching macro (mentorship), a whole fabricated 7-type CRUD library standing in for a real 20-macro logistics engine (supplychain), a dead "Advance" button cycling a fake status label with no render ever run (animation), a share link that 404'd on every use while its UI implied success (animation), quick-tools that read/wrote backend fields that don't exist so every click returned `ok:true` while rendering `undefined` (travel). Each was found by treating "the backend is real, so why does this look fake" as a red flag worth digging into, not by assuming a working-looking UI is honest. Enforcement: `server/lib/detectors/frontend-fake-data-detector.js` (registered consumer `code-quality`, run via `node server/scripts/run-detectors.js`) flags hardcoded arrays rendered as live data, `Math.random()` in render paths, and placeholder/lorem content — its strongest findings are `severity: "high"`, which the general PR ratchet (`.github/workflows/detectors-cartography.yml`) blocks on if NEW vs `audit/detectors/BASELINE.json`. A lens that's been through the rebuild loop's capability audit + step-1.5 reference-parity checklist and still ships fabricated data is a process failure, not an edge case — the checklist's GENUINELY MISSING bucket exists precisely so a missing capability gets an honest relabel or a named deferred-build disposition instead of being papered over with fake data.

**Zero generic tendencies is the companion hard invariant (2026-07-09): every button is a designed feature, never a generic action array.** Nothing about Concord is generic, so nothing in Concord's UI is allowed to read as generic. "The macro is real and gets called" is NOT sufficient — a real macro reached only through an auto-generated button wall (`<UniversalActions>`, `<LensFeaturePanel>`, a raw JSON-paste textarea standing in for a real form) is still a process failure, the same tier as fabricated data, because it means the depth is real but nobody designed for it. This is why the Frontend Rebuild Program's per-lens capability audit (step 1) classifies every macro as DESIGNED / GENERIC-STRIP-ONLY / UNSURFACED and only counts DESIGNED toward coverage — "referenced in a generic action array" is explicitly not the same claim as "a designed feature." Across every rebuilt lens this pass touched, the generic-scaffold pattern recurred as reliably as fabricated data did: raw macro-button walls standing in for real workflows, JSON-paste inputs standing in for structured forms, a page that should have been a real Bloomberg terminal / Ableton timeline / Ground News fact-checker reading instead as an undifferentiated CRUD list. Enforcement: `scripts/grade-ux-polish.mjs --honest` detects the generic-scaffold signature (`GENERIC_TRIO` — `ManifestActionBar`+`AutoActionStrip`+`RecentMineCard` — plus a `<UniversalActions>`/`<LensFeaturePanel>` body on a thin page with no substantial bespoke component) and caps it below `polished`; `server/tests/grade-ux-polish-idiom.test.js` pins the detector bidirectionally. Watch the false-positive trap the other direction: the grader does raw text matching with no comment-awareness, so a doc comment that *names* the retired scaffold components (even to explain that they were removed) can retrigger the same flag it's describing — write around it in prose, don't spell out the literal component names/JSX tags (see `docs/lens-specs/veterinary-capability-map.md` for a real instance of this self-inflicted false positive, found and fixed). **`AutoActionStrip`'s own JSON-paste textarea is fixed (2026-07-31).** The paragraph above's "raw JSON-paste textarea standing in for a real form" describes the general defect class this invariant guards against, illustrated from the historical rebuild-pass audit — it does NOT describe `AutoActionStrip` as it exists today. `concord-frontend/components/lens/AutoActionStrip.tsx` now defaults to real labeled field-row inputs (pre-populated from `GET /api/lens-actions/:domain`'s `fields` hint, generated by `scripts/extract-macro-input-hints.mjs` from ~195 informally-documented `input: {...}` comment blocks above `register()` calls, plus always-available ad-hoc rows for the ~800 macros with no hint); the JSON textarea survives only as a collapsed "Advanced: raw JSON" fallback toggle for the minority of macros needing a nested/array shape a flat form can't represent. `GENERIC_TRIO`'s detection logic is unaffected by this fix — it flags the ABSENCE of a bespoke component alongside these three, not the internal quality of `AutoActionStrip`'s own form.

**UX-polish snapshot (2026-09-07): 0.995 weighted, 264/266 lenses polished, 0 generic-scaffold detections** — `node scripts/grade-ux-polish.mjs --honest`. This supersedes the 2026-07-31 snapshot ("1.000 weighted, 265/265 polished") which is stale — the lens count grew to 266 and a live re-grade found two real, currently-open gaps, neither previously tracked anywhere in this file:
  - `strategic-adds` — **raw tier** (1/5 pillars: missing loading/empty/error/responsive states). Reads like a new lens directory that was scaffolded but never actually built out — worth checking whether it's a placeholder that shouldn't be user-reachable yet, or a genuine backlog item.
  - `chat` — capped at **functional** (5/5 pillars present, but 1 anti-pattern: a `<div onClick>` with no keyboard handler/role/tabIndex) — one accessibility fix away from polished.

  The 2026-07-31 fix this paragraph used to describe (dropping redundant generic-scaffold wrappers from creative-writing/eco/game-design) did land and those three still read clean — the regression above is unrelated, newer drift. A prior mid-session claim in this file's history of "~49 lenses still generic" was stale (predated that rebuild's completion); it is not relevant here either.

**Top-notch polish and design is the third hard invariant (2026-07-09): honest and designed is necessary but not sufficient — it also has to look and feel like a real, premium app.** A lens that passes zero-demo-content and zero-generic-tendencies can still be visually flat, under-dense, or interaction-dead — "not fake" is a floor, not the bar. `docs/UI_QUALITY_RUBRIC.md` is the enforceable spec: information density matched to data shape (§1), 3-5+ real micro-interactions per lens tied to an actual state change, never decorative hover/fade chrome (§2), a named domain visual identity per destination family — terminal/IDE/DAW/research-tool, not a generic dashboard look (§3), perceived-performance discipline (§4), and a craft checklist (§5) — all layered on top of what the grader already checks mechanically (§"What's already mechanical"). Section 0 of that doc ("Prompting for premium design") is the concrete, research-backed technique for actually getting there, not just a wishlist: name ONE precise reference app and adopt its exact visual/interaction language (not "make it nice" — "make it read like Bloomberg Terminal, specifically"); design in grayscale first and add color last, so hierarchy comes from spacing/contrast/type instead of color-as-crutch; treat whitespace as something to remove, not add — oversize it first, then trim; use the existing standardized token scale (`lib/design-system.ts`) instead of one-off spacing/type values. Enforcement today is judgment-call + orchestrator diff review at the rebuild loop's step 7 verify gate (not yet a fully mechanical detector like the other two invariants) — every rebuild-dispatch prompt from this point on must include the rubric + the premium-design technique explicitly, and the orchestrator's independent re-verification must include a real self-check against it, not just eslint/tsc/grader passing.

**Per-lens category leadership is the fourth hard invariant (2026-07-09): every one of the 260 lenses is judged as an independent product genuinely challenging the real category leader — not "good enough relative to other Concord lenses."** This is a deliberate sharpening of the reference-parity checklist (step 1.5): naming a best-in-class reference app is not enough if the bar quietly becomes "matches its feature list" — the actual bar is "would this hold up if shipped alone against Bloomberg Terminal / Linear / Ableton / EEGLAB, not just alongside 259 siblings." Concord is explicitly NOT the Unix-philosophy "do one thing well, stay narrow" model — it's the inverse ambition: do **all** 260 things well, each one treated as if it runs standalone, none of them a lesser citizen because the platform has to cover so much ground. At the same time, isolated excellence and platform composability are not in tension — every lens's real output is a first-class DTU that pipes into any other lens via the Workspace Bus (`PipingProvider`, `CitePicker`, drag/cite) and is agent-composable through the MCP server, so "isolated-caliber app" and "interoperates with the other 259" both have to be true at once, not traded off. Enforcement: the step-1.5 reference-parity checklist's disposition column must include a caliber judgment, not just a coverage checkbox ("closes the feature gap" vs. "performs at the reference app's actual quality bar" are different claims) — a checklist that's 100% ALREADY-REAL but still reads as a worse Bloomberg Terminal is not done.

**Fluidity is the fifth hard invariant (2026-07-09): a rebuilt lens must feel instant, never janky — this is a distinct claim from "polished."** Polish is what it looks like; fluidity is how it behaves under interaction. Drawn from Linear's own stated design philosophy (["the tool should get out of your way"](https://linear.app/method), keyboard-first with every action reachable without a mouse, speed and clarity treated as core features, not nice-to-haves): every mutating action gets a sub-100ms **perceived** response — optimistic UI showing the end state immediately, reconciled (quiet settle) or rolled back (visible, honest revert) when the real response lands, never a frozen control waiting on the network. Every lens's keyboard shortcuts (`useLensCommand` registrations already exist almost everywhere) must be **discoverable**, not just functional — a kbd chip, a palette entry, something a user finds without reading source, matching what §2 of `docs/UI_QUALITY_RUBRIC.md` already requires for scoped keyboard commands. Optimistic UI is NOT a license to fake success — it's still bound by the zero-demo-content invariant: an optimistic state that never reconciles with a real backend confirmation, or silently swallows a real failure, is a honesty violation wearing a performance-feature costume.

**Closing the hard 20% is the sixth hard invariant (2026-07-10): a capability-map's "Genuinely missing (deferred)" section is a work order, not a permanent resting state.** The Frontend Rebuild Program's per-lens audits repeatedly land on the same shape: the CRUD-shaped 80% of a lens is real and well-built, but the one feature that actually *defines* competitiveness against the named category leader — comps-based valuation (realestate vs. Zillow/Redfin), protocol-driven clinical decision support (healthcare vs. Epic/Cerner), real regional cost pricing (home-improvement vs. HomeAdvisor/Angi), recipient/contact search (mail vs. any real messaging product) — is the piece still missing. Documenting that gap honestly (rather than faking it) is necessary but was previously treated as sufficient; it is not. Nothing in Concord is allowed to read as a toy next to the app it's benchmarked against, and "deferred, out of scope for this pass" cannot be the final disposition for a defining feature. Every deferred gap must be triaged into exactly one of three classes before it's closed: **DATA-SOURCING** (needs a real free/open external feed — same pattern as the CPSC recall feed, the iTunes/Jamendo/Audius music ingestion, CoinGecko, USGS/NOAA/InciWeb/data.gov already wired this arc — research the best available honest source before wiring it, and if nothing free is honest-quality, say so explicitly rather than faking precision); **ENGINEERING** (a missing endpoint or feature with no external data dependency — e.g. a username-lookup endpoint — just build it); or **CURATION** (real open reference material exists but needs authoring/ingestion, e.g. seeding a clinical-protocol library from NIH/CDC/WHO published guidelines — author a real cited seed set, don't invent one). Misclassifying a CURATION or ENGINEERING gap as DATA-SOURCING wastes a research pass hunting for an API where the fix was 50 lines of backend; misclassifying the reverse ships a hand-typed, unmaintained stand-in dressed as a live feed. Enforcement: the Frontend Rebuild Program's Wave 4 gap-closure pass **has run** (executed 2026-07-12 across the lens sweep — ~24 capability maps carry per-item `CLOSED (… Wave 4 gap-closure)` markers), re-walking every lens's capability-map "Genuinely missing" section and closing each defining-feature gap per its triage class (DATA-SOURCING / ENGINEERING / CURATION). The `GENUINELY MISSING — Deferred` items that remain are the genuinely-structural ones — paid/licensed data feeds, cross-tenant substrates, hardware integrations — that are out of scope *by nature*, not pending gap-closure work. So this invariant now stands as a **ratchet against NEW deferrals** (a defining feature can't be parked in "deferred" without a triage disposition), not as an open backlog. The earlier "tracked as a standing follow-up, scheduled after the full sweep" framing is stale — the pass is done.

### 4. Metrics you can't game (the anti-cheat)
`scripts/autoloop/guard.mjs` PROTECTs the graders/baselines/harnesses (`grade-ux-polish.mjs`, `grade-macro-depth.mjs`, detector baselines) — editing them is a hard stop, and money/auth invariants are human-escalation. **The temptation to move the goalpost instead of clearing it is the #1 failure mode; the guard makes it impossible.** If a grader is genuinely wrong (a false negative, e.g. an idiom its regex misses), fixing it is allowed — but only as a **bidirectional correctness fix with a pinning test** (prove it now catches the real cases AND still fails a truly-empty one) and with explicit human authorization. Never soften a checker to make output pass; fix the code, or fix the checker's correctness with proof.

### 5. Docs are a build artifact, not prose (kill drift)
The narrative layer rots fastest and misleads worst. Numbers and structural claims must be **derived, not asserted**:
- Every `**N** … (`reproduction command`)` claim in `CLAUDE.md` + `docs/*.md` is re-run and drift-gated by `scripts/check-doc-claims-all.mjs --ci` (blocking in `.github/workflows/detectors-cartography.yml`). A count-changing PR refreshes the doc in the same commit.
- Every "pinned by `tests/…`" invariant link must resolve to a real file — `scripts/verify-invariant-test-links.mjs --ci` (a rotted proof means the invariant silently stopped being enforced).
- `docs/WIRING.md` is **generated** (`scripts/generate-wiring-doc.mjs`) from live verifier runs, with a `--check` drift gate. When you add a load-bearing doc claim, either make it generated or give it a reproduction command — never hand-type a number that has a command.
- Intent/rationale (the one irreducibly-hand-written layer) belongs **next to the code** and its load-bearing form is **pinned by a test**, so violating the intent turns a named test red.

### 6. Orchestration & safety discipline (how to run parallel work)
- Delegate well-specified, **disjoint-file** units to parallel subagents; keep the honest-failure rules + gates in every prompt. Subagents can and do refuse to fabricate — trust that, and give them the honesty rules explicitly.
- **One heavy Node process at a time on a single box** (a full `next build` + a full `node --test` together OOM and corrupt `.next`). Never run a build against a live agent's working tree.
- Stage only the **named files** you changed (`git add <paths>`, never `-A`) — a shared worktree may have another actor's in-flight edits.
- **Salvage discipline:** if an agent dies mid-unit (container hiccup), its edits are on disk — verify them (run its tests, eslint, `node --check`) and commit the unit properly; don't discard complete work or commit half-work.
- Verify before you claim done: run the affected flow, not just the typecheck. Transient regenerated artifacts (`audit/macro-depth*.json`, `reports/*.json`) are `git checkout`-reverted after a suite run, never committed.

### 7. When the plan is big: audit → research → re-audit → execute
For substantial gap-closure, the proven loop is: (a) parallel **read-only** audits of the claims (they will be wrong in *both* directions — items already done, and real gaps you didn't list); (b) **web-research** the external standard (WCAG ratios, Node handle doctrine, living-docs practice) so decisions are grounded in fact not vibes; (c) a **synthesized re-audit** cross-checking findings against the standard; (d) execute with honest, pre-agreed **stop-points** (e.g. "if the async_hooks trace can't name the sockets, ship the safe subset + document"). This is how the W/G/H-series ran — the audit repeatedly *prevented* regressions and corrected the plan's own assumptions.

### 8. "Pre-existing" is an explanation, never an excuse (owner directive, 2026-07-28)
**Establishing that a defect predates your change does not close it — it reclassifies it as debt that still has to be paid.** Determining "pre-existing" is genuinely valuable and you should keep doing it: it stops you attributing someone else's breakage to your diff, and it stops you "fixing" a symptom that was never yours. But that determination is the START of the work, not the end of it. A red test, a failing gate, or a CVE that was red before you arrived is still red *after* you leave, and the repo does not care who introduced it.

Concretely, the following are all **incomplete**, not done:
- "The 7 failures are pre-existing" → then fix the 7, or say precisely why each one can't be fixed yet.
- "That CVE came in transitively / predates this branch" → it still ships to users.
- "That gate was already red on main" → then it has been providing zero protection this whole time, which is worse, not better.

The honest report shape is **"pre-existing AND here is the fix / here is the specific blocker"** — never "pre-existing, therefore out of scope." Scoping something down is the owner's call, not yours: if you genuinely can't fix it in this pass, name it, name what it would take, and hand it back explicitly rather than filing it under someone else's problem.

This is the same instinct as §4's anti-cheat, pointed at triage instead of metrics: "not my fault" is as much a way of making a red thing look acceptable as moving a goalpost is.

---

## Commands

### Backend (`server/`)
```bash
npm run dev          # node --watch server.js (hot reload)
npm start            # node server.js (production)
npm test             # node --test tests/**/*.test.js
npm run test:watch   # watch mode
npm run test:coverage
node --test --test-name-pattern="<pattern>" tests/path/to/file.test.js  # single test
npm run lint         # eslint
npm run lint:fix
npm run migrate      # apply pending DB migrations
npm run migrate:status
npm run check-deps   # validate emergent module dependency graph (CI)
npm run smoke        # smoke test script
```

### Frontend (`concord-frontend/`)
```bash
npm run dev          # next dev
npm run build        # runs prophet-check pre-build, then next build
npm run lint
npm run type-check   # tsc --noEmit
npm run test         # vitest (watch)
npm run test:run     # vitest run (CI)
npm run validate-routes    # check route/lens manifest consistency
npm run score-lenses       # score lens implementation completeness
```

### Full stack (Docker)
```bash
docker-compose up           # starts backend + frontend + 5 Ollama instances (+ nginx, redis, qdrant, prometheus, grafana)
./setup.sh                  # first-time: deps + data dirs + .env + migrations
./startup.sh                # bare metal startup helper
```

### Repo metrics & detector gates (self-monitoring — verify, don't trust)
```bash
npm run count-loc                                          # authoritative LOC (source vs content; reclassifies data-modules)
cd server && node scripts/run-detectors.js                 # full detector suite (markdown report)
cd server && node scripts/run-detectors.js --diff --ci     # PR ratchet gate: fails on NEW high/critical vs BASELINE.json
cd server && node scripts/run-detectors.js --consumer security --diff --ci  # blocking security subset (command-injection + authz-coverage)
```
**Status (2026-06-02 honesty pass — these claims are now falsifiable, not aspirational):**
- The detector suite is a **real PR gate**, not a `continue-on-error` monitor. `.github/workflows/detectors-cartography.yml` runs the ratchet on every PR (blocks only on a NEW high/critical vs `audit/detectors/BASELINE.json`); `.github/workflows/security-detectors-gate.yml` runs the security subset as a hard gate.
- `run-detectors.js` builds an **ephemeral migrated in-memory DB**, so the db-backed detectors (`dtu-lineage`, `concordia-substrate`) actually run in CI instead of silently returning `no_db`.
- **(Historical, superseded below) 2026-08-28 refresh:** `audit/detectors/BASELINE.json` was **v2, 234 fingerprints, totals 0 critical / 11 high (8 unique fingerprints) / 20 medium / 11 low / 196 info (238)**, an owner-authorized refresh closing 3 findings that had sat reviewed-but-unapplied (2 `secret-leak` on `.env` baselined as by-design, 1 `command-injection` on `server/lib/cpu-self-pin.js:148` fixed at the code level via `execFileSync`), with 1 new `performance-hotspot` high reviewed and baselined as a genuine boot-time-only false positive. Full review in `docs/DETECTOR_DEBT_TRIAGE.md`'s 2026-08-28 entry.
- **🔴 Current state (re-audited 2026-09-07) — that snapshot no longer describes what's gating CI, and the successor was never reviewed.** `audit/detectors/BASELINE.json` on disk is now **version 1, generated 2026-09-01T11:55:45.126Z — 299 fingerprints, totals 0 critical / 50 high / 42 medium / 22 low / 196 info (310)**, `detectorCount` **51**. This file's content landed via `ea3dc18ba` ("chore: sync Mac prod dirty tree", 2026-09-07) — a generic sync commit, not a dedicated owner-authorized refresh like the 2026-08-28/08-01 ones above — and it has **no corresponding `docs/DETECTOR_DEBT_TRIAGE.md` entry**. `BUDGET.json` is still v13 `maxTotal` 460 (310 fits under it, but the headroom math was sized around the old 238 total, not this one).

  **Worse: a live `cd server && node scripts/run-detectors.js --diff --ci` re-run on 2026-09-07 FAILS even against this current committed baseline** — `added: 419 (critical=1, high=6, medium=4, low=7, info=401)`, none reviewed, none in the triage doc. (The 401 info are almost entirely `macro-usage` runtime-telemetry noise per the existing note below — the genuinely actionable ones are the other 12:)
  - **1 critical** — `maintenance-gates`: `scripts/verify-schema-drift.mjs`'s own gate is failing (a live SELECT references a column the schema doesn't have). Per this file's §8 ("pre-existing is an explanation, never an excuse"), this needs an actual fix or a named blocker, not a baseline-and-move-on.
  - **6 high** — `invariant-guardian` + `observability-gap` both flag `server/emergent/usb-lease-cycle.js`'s heartbeat handler for missing try/catch, a direct violation of this file's own "Heartbeat modules must never throw" invariant (see Key Invariants); 4× `performance-hotspot` sync-`fs`-in-async-path (`server/lib/lease-system.js:31,32,41`, `server/lib/runtime/capability-registry.js:190`); 1× `command-injection` on `.claude/worktrees/w1/server/lib/cpu-self-pin.js:148`. **That last one is a scanner-scope artifact, not a live risk** — `.claude/worktrees/w1` is a real separate git worktree on branch `organism-w1` (`git worktree list`) that predates this branch's `execSync`→`execFileSync` fix; the detector swept it because its file walk doesn't exclude `.claude/worktrees/`, not because the vulnerable code ships from here.
  - **4 medium** — 2× `unused-destructured-param`, 1× `perf_empty_catch` (`server/routes/mcp.js:483`), 1× the same `usb-lease-cycle.js` heartbeat flagged again under `observability-gap`.
  - **7 low** — 5× `stale-code` orphan modules under `server/lib/runtime/` (the DHTP-RS + adaptive-field-compression cluster) plus `server/lib/world-asset-composition.js`; 2× `performance-hotspot` (`perf_select_star_hot` in `lease-system.js:242`, `perf_unbounded_cache_growth` in `world-kernel.js:30`).

  None of this is triaged or authorized — it's open work, not settled state. Next step is the same shape as the 2026-08-28/08-01 entries: review each finding, fix-or-baseline-with-a-reason, write the `DETECTOR_DEBT_TRIAGE.md` entry, then a proper owner-authorized baseline refresh (never a silent overwrite via an unrelated sync commit). **Read the two JSON files + a live `--diff` run directly rather than trusting any number printed here, including this one** — that discipline is what caught this drift in the first place. Treat any total as a dated snapshot, not a constant — the `macro-usage` detector emits RUNTIME telemetry, so the info tier (and the total) genuinely varies run-to-run, which is why `BUDGET.json` v13 sizes its headroom to absorb that noise.
  - Of the 8 unique high fingerprints, 7 are the previously-documented `creditWallet`/`debitWallet`-class `money-txn-hygiene` control-flow-blind false positives (6 fingerprints) plus the previously-reviewed `authz-coverage` finding on the welding portal route (`money-txn-hygiene-detector.js`'s own header names the mutually-exclusive-branch pattern as a known noise class — see `docs/DETECTOR_DEBT_TRIAGE.md` rows H5/H6); the 8th is the newly-reviewed `perf_sync_fs_in_handler` boot-only false positive above. None of the 8 is a live, unreviewed defect.
  - A future BASELINE refresh is its own human-authorized step — `guard.mjs` PROTECTs that file and editing it to make output pass is the exact goalpost-moving failure mode it exists to stop. The perf backlog once cited here (73 high) is closed; see `docs/STATE_OF_CONCORD.md` §4.
- `maintenance-gates` no longer **fails open** — a missing/unrunnable verify-*.mjs gate is now itself a high finding, not a silent pass.
- `check-route-auth.js` is honest about its narrow scope (`routes/*.js` `router.*` only); the server.js global write-auth gate + `app.*` routes are covered by the `authz-coverage` detector. `tests/detector-suite-health.test.js` pins that the security consumer can't silently empty and the db-detectors can't regress to `no_db`.
- **Two new security detectors** (`server/lib/detectors/{command-injection,authz-coverage}-detector.js`) — the suite previously had ZERO injection/authz coverage, which is how PR #808's `execSync` shell-injection sink reached merge (caught by external CodeQL, not Concord's own ~30 detectors).

### Environment variables
The server requires `JWT_SECRET` in production. Without it, the boot log prints a `[FATAL]` line and falls back to an insecure default — note the `[FATAL]` is a misleading severity tag, the server continues running with a generated random secret (sessions don't survive restart). `DB_PATH` defaults to `server/data/concord.db`. `PORT` defaults to 5050. `CONCORD_NO_LISTEN=true` prevents the server from binding a port (used in tests). Five Ollama URLs (`server/lib/brain-config.js` defaults — all internal port `11434` with distinct docker hostnames; the `(11434–11438)` table that used to appear here described docker-compose host-port mappings, not in-config defaults): `BRAIN_CONSCIOUS_URL` (`http://ollama-conscious:11434`), `BRAIN_SUBCONSCIOUS_URL` (`http://ollama-subconscious:11434`), `BRAIN_UTILITY_URL` (`http://ollama-utility:11434`), `BRAIN_REPAIR_URL` (`http://ollama-repair:11434`), `BRAIN_VISION_URL` (`http://ollama-vision:11434` — LLaVA).

**Heap & cap tuning — TWO different ceilings by launch path, do not conflate them.** (This conflation caused a real misdiagnosis: a 6GB OOM was briefly waved off as "under-provisioned vs a 32GB default" when production's actual ceiling is 8GB.)
- `server/package.json`'s `start`/`dev` scripts (`npm start` / `npm run dev`) set `MAX_OLD_SPACE_SIZE=32768` and start node with `--max-old-space-size=32768 --expose-gc`. This is the figure the rest of this file's "32GB-heap" language describes — it is NOT what runs in production.
- The REAL bare-metal deploy (`pm2 start ecosystem.config.cjs --env runpod`) sets `node_args: '--max-old-space-size=8192 --expose-gc'` (applies regardless of `--env`) and `env_runpod.MAX_OLD_SPACE_SIZE: '8192'` — retuned 2026-07-20 in `ecosystem.config.cjs` for the real **9 vCPU / 50GB system RAM, single NVIDIA A40** box (the file's own audit comment walks the full RAM budget: ~20GB for 5 bare-metal Ollama processes, workers, frontend, SQLite cache, OS overhead). `max_memory_restart: '20G'` is pm2's separate, more permissive kill threshold — not the heap ceiling.
- Keep `MAX_OLD_SPACE_SIZE` numerically in sync with whichever `--max-old-space-size` CLI flag is actually launching the process for real — the memory-pressure watchdog (`server/lib/memory-pressure.js`) and `/health`'s soft-pressure flag both read the env var *separately* from the CLI flag; drift between them means pressure gets computed against the wrong ceiling.
- Both watchdogs' trim path ends in `if (global.gc) global.gc()`. Before commit `5254396e`, neither npm launch script passed `--expose-gc`, so that call silently no-oped on every trim. Fixed (both launch paths above now carry `--expose-gc`); pinned by `server/tests/invariants/launch-script-memory-config.test.js`.
- `CONCORD_MAX_SHADOWS` (default 50000) — cap for `STATE.shadowDtus`.
- `CONCORD_MAX_QUALIA_STATES` (default 50000) — cap for `STATE.qualia` (the QualiaEngine per-entity store). LRU-trimmed by the memory watchdog like `shadowDtus`; the engine returns `entity_not_found` for an evicted id and recreates on the next hook. (Each state's history is separately bounded at `HISTORY_MAX=50`.)
- `CONCORD_PLAYLIST_LIMIT` (100), `CONCORD_NPC_KNOWLEDGE_BATCH` (1000), `CONCORD_SOCIAL_BRIDGE_BATCH` (2000), `CONCORD_FAUNA_SPAWN_BATCH` (500), `CONCORD_FEED_DTUS_PER_HOUR` (10000), `CONCORD_LLM_QUEUE_DEPTH` (1000), `CONCORD_DIALOGUE_MAX_CONCURRENT` (50), `CONCORD_DOWNLOADS_PER_USER` (25).
- `CONCORD_FEDERATION_TOKEN` — when set, federation `/api/world/social-shadows` requires Bearer auth.
- **`LLM_CONCURRENCY` (default 32, `_llmQueue` in `server.js`) — reviewed 2026-07-27 for headroom on the A40 box, left as-is.** The 7 Ollama containers' `OLLAMA_NUM_PARALLEL` values sum to **42** (each per-container setting, via `grep -E 'OLLAMA_NUM_PARALLEL=[0-9]' docker-compose.yml | grep -oE '[0-9]+' | awk '{s+=$1} END {print s}'` — 4+6+8+8+6+6+4), so 32 is already close to that ceiling, not far under it. `docker-compose.yml`'s own "Four-Brain Cognitive Architecture" comment block states the box's real constraint plainly: ~7.7GB of VRAM headroom in 48GB, but "the scarce resource on this box is CPU, since 7 Ollama containers plus the rest of the stack ... share only 9 cores total" — no comparable CPU slack exists. Raising `LLM_CONCURRENCY` past 32 would not convert into more completed work under that constraint. The real lever for more effective throughput is registering High Power Mode's platform providers (Groq/Gemini/Mistral) as additional `pickBrainEndpoint` candidates, so overflow load spills to an operator-funded external endpoint instead of competing for the same 9 cores — see `server/lib/brain-config.js#pickBrainEndpoint`'s `includeCloud` option. (Separately, `_CONCURRENCY.limits.llm_call` in `server.js` — an older, different Tier-2 rate-limit gate, default 64 via `LLM_CONCURRENCY_LIMIT` — was tuned for the prior 32GB-heap/RTX-PRO-4500 target and is a distinct subsystem worth its own revisit; out of scope here.)

---

## Architecture

### The monolith: `server/server.js`
**87751 lines** (`wc -l server/server.js`, re-verified 2026-09-08 — was 85,366 on 2026-08-29; grows steadily, so treat this as a fast-moving number, not a constant). All routes, middleware, startup, and tick logic live here. It is intentionally monolithic (comment in code: "for IP protection"). Adding new routes means adding them directly to this file. It imports from `server/lib/`, `server/emergent/`, `server/domains/`, and `server/routes/`.

**Boot-order TDZ hazard.** `const app = express()` is at `server.js:35894` and `const LENS_ACTIONS = new Map()` is at `server.js:46256` as of 2026-09-07 (was 27554/36537 — both had drifted ~8-10k lines by the time this was re-checked; grep, don't trust either number) — code at the top of the file that references either at module-eval time hits a temporal-dead-zone ReferenceError. Two mount blocks (`mountChatAgentStream`, `mountMcpServer`) sat ~4k–13k lines too early and silently dead-mounted `/api/chat-agent/stream` + `/mcp` for months until commit `7e83685` moved them to right after `LENS_ACTIONS`. Sprint 18.5 had deferred the `__concordLensActions` globalThis assignment for the same reason. **New code that references `app` or `LENS_ACTIONS` at top-level must sit after their declaration, or be wrapped in a function that runs post-boot.**

### Three-gate permission system
Every frontend API call passes three gates in `server.js`:
1. **authMiddleware** — `publicReadPaths` array (path prefix allowlist for unauthenticated GET)
2. **runMacro** — `publicReadDomains` object (domain+macro allowlist)
3. **Chicken2** — `_safeReadPaths` + `safeReadBypass` boolean

### The macro system
Frontend calls `POST /api/lens/run` with `{ domain, name, input }`. This routes to `runMacro(domain, name, input, ctx)` in `server.js`. Direct grep at HEAD `21c8f34`:
- **~558 unique macro domains** (re-verified 2026-09-07 via the verifier — see below; was 547 on 2026-08-01). NOTE: the old `awk -F"'"` direct-grep is methodology-fragile — it breaks on double-quoted registrations (returns 14 on the current tree), so it's superseded; trust the verifier's `macroDomains` field.
- **10,632 unique `(domain, macro)` pairs** (re-verified 2026-09-07; was 10,399 on 2026-07-25); per-domain counts skew heavily — `accounting` ~118 macros, `projects` ~99, `game-design` ~98, `code` ~81, `healthcare` ~82. The earlier "~826 / ~429" range in this file came from a stricter grep that missed many of the dynamically-named pairs.
- The verifier in `scripts/verify-lens-backends.mjs` reports 558 macro domains and 3,042 route prefixes (`macroDomains`/`routePrefixes` fields) — it walks the tree including `register*` variants. This is the reproducible source of truth for the domain count.

Wiring-wise (verifier at HEAD `21c8f34`): every lens but 4 reaches a registered macro or REST route — see "Missing (needs building)" below for the four exceptions. Domain logic lives in `server/domains/<domain>.js`; some domains additionally register macros inline from `server/server.js` or other top-level files like `server/guidance.js`.

### Heartbeat tick (every 15s)
`governorTick()` in `server.js` drives all emergent simulation. Two registration patterns coexist:
- **Per-entity inline ticks** (**237** module files in `server/emergent/*.js` — `ls server/emergent/*.js | wc -l`; was 236) running at varying frequencies (every tick, every 5th, every 20th, etc.).
- **Singleton periodic modules** registered via `registerHeartbeat(name, { frequency, handler })`. **143 unique heartbeats in production code** (re-verified 2026-09-07, `--exclude-dir=tests`; was 140 on 2026-08-01, 127 at the 2026-06-07 count, 68 at the Phase-12 audit — the count keeps growing with each sprint, so re-count with the grep below, don't trust a stale snapshot) — `grep -rohE "registerHeartbeat\\(['\"][a-z0-9-]+['\"]" server/ --exclude-dir=tests | sort -u | wc -l`. **The `--exclude-dir=tests` matters** — running the bare command without it (as this line used to specify) sweeps `server/tests/*` mocks/fixtures too and returns 179, of which 36 unique names exist only in test files and were never real production heartbeats; the command in this file was itself wrong, not just the number. The game-plan-completion T-series added `scheme-overhear-cycle` (8), `world-zone-hazard-cycle` (5), and `population-migration-cycle` (30, the T2.4 orphan that was wired). **Dispatcher wire (Phase 12 fix)**: registrations build a registry, but they don't run until `_startGovernorHeartbeat()` is called — which schedules `governorTick()` on a 15s `setInterval`, which calls `tickAllRegistered()` to dispatch each module whose tick is due. Wired at `server.js:37656` as of 2026-09-07 (was 29105 — `_startGovernorHeartbeat`'s own definition is further down at `server.js:41522`; boot+50s setTimeout). Prior to the Phase 12 audit this call was missing and all 68 registry-style heartbeats silently never fired. Highlights: social-npc-bridge (5), npc-knowledge-bridge (10), metrics-decay (20), fauna-spawner (30), creature-flock-cycle (4), signal-propagation-cycle (3), eco-expiry-sweep (5), refusal-field-sweep (1), corpse-cleanup (10), repair-cycle (20), environment-sensor (5), embodied-dream-cycle (80), forward-sim-cycle (100), faction-strategy-cycle (200), lattice-{drift-scan,breakthrough-pass,federation-poll,quest-cycle}, npc-{conversation-initiator,routine-cycle,economy-cycle,marketplace-cycle,skill-evolve-cycle}, npc-perception-snapshot (8), npc-scheme-cycle (30, Sprint D A4), kingdom-decree-cycle (16, Sprint D D2), procgen-settlement-cycle, personal-beat-scheduler, season-cycle, land-claims-cycle, mount-care-cycle, player-signs-cleanup (240), combat-recovery-cycle, code-substrate-refresh, detectors-sweep, procedural-npc-spawner, reflex-{architectural-drift,dependency-entropy,scaling-pressure,unsafe-expansion}, understanding-evolve, brain-daily-refresh, brain-outcome-resolver, scheduled-posts (4), affect-tick, qualia-persist, culture-drift-pass, forgetting-health-check, presence-stale-sweep, environment-sense.

Always wrap new heartbeat additions in `try/catch` — a module crash must never stop the tick. Counter `concord_heartbeat_ticks_total` (server.js:8665 as of 2026-09-07 — this and most other bare `server.js:NNNNN` citations in this file drift fast and shouldn't be trusted without a grep; see the Architecture section's TDZ-hazard note, alert `ConcordHeartbeatStopped` in `monitoring/prometheus/alerts.yml:60`) increments per tick; rate==0 for >60s indicates the loop has frozen. Skipped ticks (when a previous tick is still running) are NOT counted — observable gap.

### DTU substrate
Discrete Thought Units are the atomic knowledge format. Four layers: `human` (readable summary), `core` (structured claims/definitions), `machine` (tags, embeddings, verifier), `artifact` (optional binary at `./data/artifacts/{dtuId}/`). Regular DTUs consolidate into MEGA (5–20 originals) then HYPER (50–200) at ~1.2× measured DHTP compression (~1.2× (was 33:1 target) was a target) every 30 ticks. There is **no hard DTU ceiling**; memory pressure is governed by `server/lib/memory-pressure.js` against `MAX_OLD_SPACE_SIZE`. With the 32GB `npm start`/`dev` heap the substrate comfortably holds ~1.5M DTUs; the real bare-metal production ceiling is 8GB (see "Heap & cap tuning" above), so this capacity figure does not describe the deployed box as-is.

### Five-brain architecture (four cognitive + LLaVA vision)
Code-level fallback models (`server/lib/brain-config.js`, table below) were originally tuned for the **NVIDIA RTX PRO 4500 Blackwell** (32GB GDDR7) target this project started on. **GPU/CPU pinning audit (2026-07-20): the real deployed target is now a single NVIDIA A40 (48GB GDDR6, Ampere), not the Blackwell card** — `docker-compose.yml` (search "target: single NVIDIA A40") documents this in detail, including a real VRAM budget (~40.3GB committed of 48GB across 7 Ollama service instances, ~7.7GB headroom) and per-instance `OLLAMA_NUM_PARALLEL`/`OLLAMA_MAX_LOADED_MODELS` tuning sized for the box's 9 vCPU / 50GB RAM. The A40 deployment overrides the code-level conscious-brain default to a larger 32B-q4_K_M model via `BRAIN_CONSCIOUS_MODEL` and horizontally duplicates the subconscious/utility instances (`ollama-subconscious-2`, `ollama-utility-2`) to use the extra headroom — a deployment-level tuning choice on top of, not a change to, the code defaults below. `docs/OPERATOR.md` and `docs/DEPLOYMENT_TOPOLOGY.md` still describe the stale Blackwell target and should be read with this correction in mind. Override any model via env var.

| Brain | Default model (`server/lib/brain-config.js`) | Port | Role |
|---|---|---|---|
| Conscious | `concord-conscious:latest` (custom model built on qwen2.5) | 11434 | Chat, deep reasoning, council |
| Subconscious | `qwen2.5:7b-instruct-q4_K_M` | 11435 | Autogen, dream, synthesis |
| Utility | `qwen2.5:3b` | 11436 | Lens actions, quick tasks (65% of requests) |
| Repair | `qwen2.5:1.5b` | 11437 | Error detection, auto-fix |
| Vision | `qwen2.5vl:7b` (Qwen2.5-VL, Apache-2.0) | 11438 | image understanding, food vision, doc layout. Swapped off `llava:13b-v1.6-vicuna` (Vicuna→LLaMA + GPT-4-instruction-data CC-BY-NC lineage = commercial exposure) to a cleanly-commercial Apache-2.0 model that unifies the stack on Qwen. Override with `BRAIN_VISION_MODEL`. See `docs/LICENSING.md`. |

All five Ollama services run with `OLLAMA_FLASH_ATTENTION=1` + `OLLAMA_KV_CACHE_TYPE=q8_0` to use the GPU's tensor cores and halve KV cache memory (both Blackwell and the current A40/Ampere target support this). `initFiveBrains()` probes all five (4 cognitive + 1 multimodal/vision) on startup and auto-pulls models. `ctx.llm.chat()` routes to conscious; falls back to subconscious. **There is no OpenAI emergency-fallback path** — when a user supplies an external API key, their individual brain slots route per-slot through the BYO key router (migration 170 `byo_brain_overrides`); the prior OpenAI relics were removed. Vision queries route through `server/lib/vision-inference.js#callVision` which reads `BRAIN_VISION_URL` and routes via `BRAIN.multimodal` / `BRAIN_CONFIG.multimodal`.

### 253-lens frontend
`concord-frontend/app/lenses/` has **267 directories** (verified by `ls -d concord-frontend/app/lenses/*/ | wc -l`) — that count is **266 lens directories** plus one Next.js `[parent]` layout directory; subtract `[parent]` for the lens-only count. The Phase D + Z1 additions are `courtship`, `fishing`, `creatures`, `garage`, and `reasoning/traces`. Full per-dir inventory in `docs/AUDIT_INVENTORY.md`, depth scoring in `docs/PHASE12_AUDIT_lens-classification.csv`. Each lens page calls its backend domain macro via `POST /api/lens/run` with `{ domain, name, input }`. Lens feature specs live in `server/lib/lens-features.js` and `server/lib/lens-features-extended.js`; run `npm run score-lenses` to audit implementation completeness against the manifest. Many lenses also mount a **rival-shape silhouette** (e.g. code → VSCodeShell, music → SessionView, accounting → KPIStrip, atlas → GraphView, message → InboxShell, crypto → WalletShell, healthcare → EHRShell, whiteboard → WhiteboardCanvas) so each lens reads as the app it replaces while sharing one substrate.

**The 259 lenses are presented through ~25 concentrated "destinations"** (2026-06-08; architecture detail in `docs/CONCORD_DESTINATIONS.md`). The front door is **Core 6 + a grouped "Destinations" tier** (`lib/destinations.ts` + `components/shell/Sidebar.tsx`), each destination composed from two knobs — **absorbed sub-lenses as workspace tabs** (`components/common/DestinationNav.tsx`) **+ cross-mounted panels** (`lib/panel-registry.ts` + `lib/panel-affinity.ts`, ~700 self-contained panels addressable by id) — while **all 259 stay reachable** via Hub / ⌘K / sub-lens tree. Composition is how a destination gets depth: Finance absorbs `markets`/`wallet`/`staking`/`ledger` and cross-mounts crypto/accounting/energy panels. This is the mechanism, stated factually — make your own call on how it reads.

### Concordia (World Lens)
3D civilization simulator inside the platform. Key directories:
- `concord-frontend/lib/world-lens/` — Three.js terrain, building, avatar, physics (28 TS files, ~428KB)
- `concord-frontend/lib/concordia/` — Gait synthesis, FABRIK IK, secondary physics, combat logic
- `concord-frontend/components/world/` — IsometricEngine, AvatarSystem3D, CombatHUD, GameJuice
- `concord-frontend/components/concordia/` — HUD, skills, dialogue, quests, world UI
- `server/lib/npc-*.js` — 11 NPC modules (simulator, behaviors, archetypes, family, gear, jobs, relations, spawning)
- `server/emergent/quest-engine.js` — In-memory quest engine (createQuest, prerequisite chains, breadcrumb protocol)
- `server/lib/oracle-brain.js` — LLM quest chain + dialogue generation
- `server/lib/narrative-bridge.js` — Enriches oracle-brain calls with authored NPC/faction context
- `server/lib/content-seeder.js` — Seeds authored world content at startup (idempotent)
- `content/world/` — Authored factions, NPCs (with backstories), lore events
- `content/quests/` — Authored quest chains (onboarding, 7-quest main arc, 8 faction quests)

### Database
SQLite via `better-sqlite3` (in `dependencies`, not optional — the server hard-requires it). Synchronous, in-process, no ORM. Migrations in `server/migrations/` — **443 numbered migration files** (`ls server/migrations/[0-9]*.js | wc -l`; highest number **442**, `442_dtu_store_visibility_columns.js` (Concurrency Refactor Phase 3 — dtu_store visibility columns for the off-loop DTU read sidecar), preceded by `441_bench_column.js`, `440_dtu_cognitive_schema.js`, `439_cognitive_compiler_v2.js` — re-verified 2026-09-08). Distinct CREATE TABLE statements: **825** (`grep -hoE "CREATE TABLE\\s+(IF NOT EXISTS\\s+)?[a-z_]+" server/migrations/*.js | sed -E 's/.*CREATE TABLE\\s+(IF NOT EXISTS\\s+)?([a-z_]+).*/\\2/' | sort -u | wc -l`). Schema version on the running DB tracks the highest applied migration, in the `schema_version` table. Run automatically at startup and manually via `npm run migrate`. Numbers 141 and 143 are `_drop_dead_mig*` debt-cleanup migrations renumbered to dodge collisions — a real numbering gap, not a missing file. **Full migration-by-migration ledger (what table each number added and why, migrations 100-441)**: [`docs/CLAUDE_MD_ARCHIVE_MIGRATION_HISTORY.md`](docs/CLAUDE_MD_ARCHIVE_MIGRATION_HISTORY.md) (moved out of this file 2026-09-07 for size, verbatim) and `docs/AUDIT_INVENTORY.md`.

### Godot client (`world-lens-godot/`)
A real Godot 4 client (71 `.gd` files) for Concordia, parallel to the Three.js web client, not a replacement — this directory previously went entirely unmentioned in this file (a doc-gap found and fixed 2026-08-01). Connects over a raw WebSocket at `/godot-ws` (`server/lib/godot-gateway.js`, `mountGodotGateway`, mounted in `server.js`) with real auth (JWT/API key), rooms, and a shared server-authoritative combat path — `mountGodotGateway`'s `onClientMessage` is required to run through the IDENTICAL validation the socket.io path uses, not a parallel trust boundary. `net/gateway_client.gd` does real per-connection `_seq` monotonicity checking (`_seq` is stamped from a single module-instance-lifetime counter server-side, so it is NOT reliable as a cross-reconnect gap detector — read the file's own class doc before assuming otherwise) and drives reconnect resync. A first shippable milestone — a read-only spectator viewer — shipped in the same pass (PR #875, commit `69b42627`). `npm run export:web` (`scripts/export-godot-web.mjs`) fetches the pinned engine + web export templates, runs the mandatory-separate import pass (godotengine/godot#77508 landmine), and exports to `concord-frontend/public/godot-client/index.html`, which the frontend's existing static-file serving already reaches with zero new routing (confirmed: `public/` already serves large binaries unmodified). No in-app lens page embeds it yet (no iframe convention exists anywhere in this repo) — that is a separate, undecided UI/product-shape question, not a serving gap. Full protocol + runtime detail: `docs/GODOT_INTEGRATION.md`, `docs/GODOT_PROTOCOL.md`, `docs/GODOT_RUNTIME.md`.

### Mobile
`concord-mobile/` — React Native + Expo v52. Real native app with BLE, WiFi P2P, geolocation, NFC, SQLite local store, wallet/marketplace. Not a web wrapper. Secure storage uses `expo-secure-store` (iOS Keychain / Android Keystore) on native and `WebCrypto` AES-GCM with a non-extractable key in IndexedDB on web — selected by `createSecureStorageForPlatform(Platform)` in `App.tsx`.

---

## Current Wiring Status (post-Concordant Web)

This section exists so future sessions don't repeat discovery work.

### Verified ground truth — `docs/AUDIT_INVENTORY.md`

The cartographer outputs (`audit/cartograph/SYSTEMS.json`, `SYSTEMS.md`, `CARTOGRAPH_WIRING_STATUS.md`) are advisory and have drifted historically. **For machine-truth counts, run `docs/AUDIT_INVENTORY.md`'s reproduction commands directly.** Numbers below are from the most recent direct grep against the working tree:

Direct-grep counts **re-verified 2026-09-07** (superseding the 2026-06-02 pass below — every count had grown again; this table drifts fast enough that it should be re-run, not trusted, whenever a number here matters):

| Surface | Count | Reproduce |
|---|---|---|
| Frontend lens directories | **267 dirs total** (266 lenses + 1 `[parent]` Next.js layout dir; the script counts both — subtract 1 for lens-only) | `ls -d concord-frontend/app/lenses/*/ \| wc -l` returns 267 |
| Lens backend wiring | **262 WIRED / 0 broken / 4 NO-BACKEND-CALL (2 by-design, 2 verifier false-negative)** (re-run 2026-09-07; was 263/0/2 on 2026-08-01 — see the Wiring status paragraph above for the `ar`/`art` false-negative detail) | `node scripts/verify-lens-backends.mjs` |
| Backend domain files | **440** (was 434) | `ls server/domains/*.js \| wc -l` |
| Numbered migrations | **444 total `.js` files** in `server/migrations/` (443 numbered `NNN_*.js` files — highest number `442` — plus 1 non-numbered `_drop-with-rescue.js`) | `ls server/migrations/*.
| Route files | **136** (was 132) | `ls server/routes/*.js \| wc -l` |
| Emergent modules | **237** (was 236) | `ls server/emergent/*.js \| wc -l` |
| Lib modules | **783** top-level (was 782) (`ls server/lib/*.js \| wc -l`) · **1312** recursive (was 1281) (`find server/lib -name "*.js" \| wc -l`) | — |
| `server/server.js` line count | **87751** (was 85366) | `wc -l server/server.js` |
| HTTP routes (server.js + routes/*.js) | **~3,435 total** (1,467 + 1,968; was ~3,371 = 1,414 + 1,957) | `grep -cE "^\\s*(app\|router)\\.(get\|post\|put\|delete\|patch\|all)\\(['\"]/" …` |
| Unique macro domains | **558** (verifier `macroDomains`; was 547) | see "macro system" section above |
| Unique `(domain, macro)` pairs | **10,632** (was 10,399) | `grep -rhoE "\\b(register\|registerLensAction)\\(['\"][a-zA-Z0-9_.\\-]+['\"]\\s*,\\s*['\"][a-zA-Z0-9_.\\-]+['\"]" server/ \| sed ... \| sort -u \| wc -l` |
| Distinct CREATE TABLE statements | **825** (was 747) | see Database section |
| Unique heartbeats | **143** in production code (was 140; the bare grep with no `--exclude-dir=tests` returns 179, inflated by 36 test-only names — see the Heartbeat tick section) | `grep -rohE "registerHeartbeat\\(['\"][a-z0-9-]+['\"]" server/ --exclude-dir=tests \| sort -u \| wc -l` |
| Authored source LOC | **~2.77M** (4.83M incl. content; was ~2.74M / 4.51M — re-run 2026-09-08, includes the Go/Rust concurrency sidecars) | `npm run count-loc` |

**Keeping this table honest:** `npm run check-doc-claims` extracts every `(bold number, reproduction command)` pair in this file, re-runs the command, and reports drift (`--ci` to fail the build). The 2026-06-02 refresh above was driven by it. Re-run it before trusting any count here; the `docs/AUDIT_INVENTORY.md` file is older still and is superseded by these.

### Fully working end-to-end / Built but not wired / Missing

Archived verbatim to [`docs/CLAUDE_MD_ARCHIVE_WIRING_DETAIL.md`](docs/CLAUDE_MD_ARCHIVE_WIRING_DETAIL.md) (2026-09-07, purely for CLAUDE.md size budget — nothing rewritten). Read it before touching Layers 7-13 (embodied signals/pain/dreams/forward-sim/faction-strategy/lattice-orchestrator/NPC-conversations), the frontend/backend contract-checker map, or anything under "Missing (needs building)". Short version: 0 built-but-unwired items, 0 missing-critical items (content census AT TARGET, POLISH_AUDIT Tier-0 defects fixed except the noted PvP seam — check the archive for current status).

---

## Recent shipped work (most → least recent)

Archived verbatim to [`docs/CLAUDE_MD_ARCHIVE_SHIPPED_WORK.md`](docs/CLAUDE_MD_ARCHIVE_SHIPPED_WORK.md) (2026-09-07, purely for CLAUDE.md size budget — nothing rewritten). It's the full per-sprint changelog table (Codebase audit / ConKay prod-audit / Living Society / Depth & Balance / Game-plan completion / Phase Z / Concordia game-feel / Phase 6a-6d / Phase 1-5 / Concordant Web / EvoEcosystem / v2.0 / Audit pass). Read it for "what landed and where" on any past sprint; current invariants live below, not there.

---

## Key Invariants

- **Depth-continuation invariants (2026-05-29, D5/D6/E-series):**
  - **CK3 hooks are leverage held OVER a target.** `server/lib/hooks.js` (mig 277 `npc_hooks`). A *strong* hook held by the TARGET over a PLOTTER blocks the plotter's scheme (`blocksHostileAction` → `proposeScheme`/`proposePlayerScheme` return `reason:'hooked'`); a hook the plotter holds over the target raises `success_pct` (`successBonusFor`, floored into the base). A *weak* hook is single-use coercion (`coerce` spends it, records `COERCE_OPINION_DELTA −12` resentment); the `interveneInScheme` `blackmail` branch spends a held hook to force a scheme to `abandoned`. **Inheritance is load-bearing**: `onNpcDeath` calls `inheritHooks` — hooks over the deceased re-point (decayed) to the heir, hooks the deceased held pass on. All guarded (table-optional). Secret discovery (`secrets.discover`/`surveillance_roll`) yields a hook via `generateHookFromSecretDiscovery` (strong if difficulty ≥7 or corroborated). Dials in `docs/BALANCE_DIALS.md` (`CONCORD_HOOK_*`).
  - **Run-meta banks into `roguelite_meta_currency` for ALL modes.** `run-difficulty.js#grantRunMeta` is the single Hades gem bank; horde/extraction/roguelite all pay into it on a LOSS so a wipe still advances. Roguelite payout = base × `max(1.0, tier loot_mult)` — **floored at 1.0** so the default/finder path (seeded loot_mult 0.5) is never reduced; only heroic/mythic amplify. Never reduce the default payout by applying the raw finder mult.
  - **Spouse reactivity is deterministic + estranges.** `spouse-reactivity.js#reactToPlayerEvent` shifts courtship affinity off the spouse's own faction+opinions (never RNG); affinity below `CONCORD_SPOUSE_ESTRANGE_THRESHOLD` (−0.3) flips a marriage to `estranged` + dissolves it. `player_marriages` dissolution column is `dissolved_reason` (NOT `dissolution_reason`).
  - **NPC dialogue has a deterministic fallback.** `npc-dialogue-fallback.js#composeDeterministicDialogue` is the default greeting/subtext/mood in `/dialogue` BEFORE the LLM override — so an LLM-off box still reads as a person. It NEVER includes secrets (presence flags only). Don't revert the default back to the flat `interactResult.greeting`.
  - **Code-puzzle VM accepts both instruction shapes.** `programming-puzzle.js#_normalizeInstr` maps the editor's `{op,a,b}` → canonical `{dst,src,to}` per op; explicit canonical fields are left untouched. The UI ships `{op,a,b}` — do not "simplify" the VM back to reading only `dst/src/to` or every UI program silently no-ops again (POLISH_AUDIT T0.1).
  - **Juice SFX ids are normalized.** `SoundscapeEngine.tsx#resolveSfxId` maps the underscored juice ids (`ui_*`) onto the hyphenated SFX_MAP voices. New juice sfx ids should add an alias (or follow the `_fail`/`_pass`/`_open` heuristic) — a raw `SFX_MAP[id]` lookup on an underscored id is silent (POLISH_AUDIT T0.2).
  - **Client cadence dials come from `/api/config/client`.** `useClientConfig()` merges server values over baked defaults; new polls should read the hook, not hardcode a `POLL_MS`. The dials are presentation-only, never gameplay/economy.

- **MMO sprint invariants (Phase U-W):**
  - **Mail attachments transfer is single-transaction.** `server/lib/player-mail.js#claimAttachments` runs COD walletDebit → walletCredit + DTU ownership transfer inside one `db.transaction(...)` call. Partial failures roll back; idempotent on already-claimed.
  - **Auctions extend by 60s on snipe.** A bid landing in the final `CONCORD_AUCTION_SNIPE_WINDOW_S` seconds (default 60) pushes `ends_at` forward by `CONCORD_AUCTION_SNIPE_EXTEND_S` (default 60). The settlement heartbeat runs every ~1min; never settle an auction with `ends_at > now`.
  - **Achievement unlocks are idempotent on `(user_id, achievement_id)`.** The PK collision in `INSERT INTO player_achievements ... ON CONFLICT DO NOTHING` means re-evaluating the same event is safe; reward CC + title are awarded only on the first INSERT.
  - **Diseases never cross worlds.** Contagion radius spread uses `getPlayersInCell` (Phase M) scoped to the patient's current world. `player_diseases` rows don't replicate; immunity (`disease_immunity` table) is global per (user, disease).
  - **Faction reputation is cached.** `player_faction_reputation_cache` (migration 218) is refreshed by the `faction-rep-cache-refresh` heartbeat (freq 60). Direct gameplay reads should call `getFactionReputation` which falls back to live `computeFactionReputation` when the cache is empty.
  - **Title display goes through `friend-presence.resolveUserDisplay`.** Don't query `users.active_title_id` directly from the frontend — call `/api/friends/presence` or the equivalent helper that already joins the title.
  - **Plague threshold is 0.15.** When >15% of a world's recent visitors have an active disease severity > 0.3, `world:plague-declared` fires. Resolves when ratio drops below 7.5%. Override via `CONCORD_PLAGUE_THRESHOLD`.
  - **LFG cancels prior-open from same user in same world.** `postLfg` doesn't allow two concurrent requests in one world — the new post auto-cancels the old.
  - **Party leadership transfers to earliest joiner** on leader leave; party disbands if 0 members remain. Leader-only actions: invite (on invite_only), kick, disband.

- **Convergence sprint invariants (Phase CA-CF):**
  - **Flight HUD reads `concordia:flight-state`.** AvatarSystem3D Phase B3 dispatches the event; FlightHUD.tsx is the consumer. 2s silence auto-hides. New flight features should extend the event payload, not add a parallel channel.
  - **Submarine HUD polls `/api/players/me/dive-state`** which joins world_visits + player_oxygen + creature_swim_depth. Sonar range 80m horizontal + 8m depth band. Don't query the three tables independently from the frontend.
  - **Climbing routes record on stamina-state transition, not on every frame.** When `player-stamina.js#setState` goes climbing → rest (or exhausted), the caller invokes `lib/climbing.js#recordRoute`. Height = max(0, peak_altitude − start_y). first_summit + cliff_master achievements consume this.
  - **Detective lock-in is 2-of-3 with suspect-match required.** `lockInDeduction` resolves the case only when `correctCount >= 2 AND reasons.includes('suspect_match')`. Pure weapon+motive without suspect won't solve it. Each attempt persists into `arrest_records`.
  - **Brawl mode is in-memory only.** `lib/brawl.js` uses Maps, not tables — invites are ephemeral (60s TTL), active brawls clear on `endBrawl`. The profile applied is always `sifu_brawler` (purpose-built for fist 1v1).
  - **Roguelite currency is separate from CC wallet by design.** `roguelite_meta_currency` is the Hades-pattern gem/key bank. Never auto-converts. `roguelite_unlocks` PK is (user, unlock_id) — re-purchase rejected as `already_unlocked`.
  - **Roguelite runs end on region-exit, death, OR timeout.** `startRun` in a different region closes the prior run as `timeout`. One active run per user.
  - **Horde mode caps wave-based spawn rate via `spawnRateAtWave`** = BASE × 1.25^(wave−1). `horde_runs.auto_attack` defaults 1 — the combat route ticks damage on cooldown when this is set. Upgrade choices are deterministic per `(runId, picks_count)` so re-roll returns the same set.
  - **Farm crops only grow during their season affinity.** `claim_crops` rows with `crop_kind` whose `seasons[]` excludes the current `season_idx` halt growth at their current stage. Wrong-season planting is allowed but pointless until the season returns.
  - **Restaurant orders expire at 5min by default.** Diner-Dash pressure: serve within 30s = 30% tip; within `expires_at − 60s` = 10% tip; later = 0. Expired orders increment `orders_missed`.
  - **Trivia questions are DTUs.** The answer is `answer_dtu_id`; the player's submission is a `cited_dtu_id`. Correctness = string equality. The royalty cascade fires when the citation is registered (existing path, unchanged).
  - **Hidden object bbox is inclusive on lower bound.** `x >= t.x && x <= t.x + t.w` (same for y). Already-found targets are filtered before the bbox check, so multiple clicks on the same spot don't double-credit.
  - **The canonical combat is Skyrim-style action, NOT RTwP.** Real-time keyboard input (E light/heavy, F parry/grab, R kick, Q dodge, Shift modifier) flows through `concord-frontend/components/world-lens/CombatInputController.tsx` → socket events `combat:attack` / `combat:dodge` / `combat:block` / `combat:grab` / `combat:kick`. No pause, no turn queue. Procedural animation is the load-bearing system: `combat-biomechanics.ts` (488 LOC, 7-phase punches/kicks computed from joint range tables — Winter 2009 / Perry & Burnfield / Dempster 1955 — tier-scaled T1..T5), `combat-motor-driver.ts` (210 LOC, PD motor targets via `JointMotorSystem` replacing keyframes), `reflex-layer.ts` (200 LOC, recipient wince/stagger). **Correction (2026-05-29 audit, `docs/POLISH_AUDIT.md`):** the live animation path is the **baked biomechanics clips** (`buildBiomechClipMap` → `AvatarSystem3D.handleCombatAnim`); `combat-motor-driver.ts` and `reflex-layer.ts` were **dead-wired** — their bridges (`CombatMotorBridge.tsx`, `ReflexBridge.tsx`) subscribed to a client event the server never broadcast, passed empty poses, bound no skeleton, and emitted nothing (a per-frame rAF for zero effect). **Resolved 2026-05-29 (D1 depth plan): the two bridges were RETIRED/removed** (the "or remove them" option was taken — `app/lenses/world/page.tsx:432` records they "emitted `concordia:combat-pose-targets` with zero consumers"); the avatar loop's baked clips + the live `combat:impact` momentum path below are the real animation surface. Impact is continuous bone-mass × angular-velocity-at-contact via `computeImpactMomentum(boneMass, angularVelocity, leverArmM)` — there are NO AABB hitboxes; the mechanism is bone-level momentum at contact frame. **This claim is now true in the live path (T1.4a/b), not the dead-code it once described:** `server/lib/combat-impact.js` computes the momentum server-authoritatively and `triggerStaggerFromImpact` resolves a deterministic poise-vs-momentum stagger (graded flinch→rocked→knockdown) that **replaced** the old `stagger_chance` dice roll; `server/lib/combat/impact-feel.js` maps that severity to the client hitstop/knockback/wince it ships on `combat:impact`. Frame-data timing windows (parry/dodge/active) still gate *whether* contact lands; momentum governs *what it does* — the two compose. Lock-on (soft + hard) via `LockOnController.tsx` + `cameraLookState.lockedTargetId`. Server-side anti-cheat (`_validateCombatReach` + `_validateDamageCap`) gates `/api/worlds/:worldId/combat/attack`. **If you find yourself building turn-based or RTwP-pause combat as the primary surface, you're misreading the design — action is the canonical mode.**
  - **Party combat (`/api/party-combat/*`) is a SEPARATE optional tactical substrate.** Real-time-with-pause shape (FF7 Remake / BG3 RTwP), used by the `PartyCombatHUD` (DB9) when the player is explicitly inside a `party_combat_sessions` row. Each combatant has `next_action_at_ms` — a wall-clock cooldown gating their next queued action. Pause via `setTimeScale(0)` (the `concord-frontend/lib/concordia/use-time-scale.ts` PhotoMode primitive). `resolveTick(sessionId, nowMs)` walks combatants whose cooldown elapsed and fires their queued action. Damage clamps to `DAMAGE_CAP_HARD = 500`. The orphaned `turn_combats` tables from migration 251 are harmless dead substrate — do NOT write to them. Tactical-party code uses migration 259 `party_combat_sessions` + `party_combatants` + `party_queued_actions` + `party_action_log`. **This substrate exists for party-tactics encounters only — it never replaces the action combat path above.**
  - **Hacking `getPuzzle` does NOT leak `solution_path_json`** — explicit column list. New code that adds a SELECT * here is wrong. The solution is server-private; only `attemptCommand` validates against it.
  - **Programming puzzle VM caps at MAX_CYCLES = 10_000** per test case. Infinite loops terminate. Submitted programs only persist when ALL test cases pass.
  - **Factory entities live inside `land_claims` only.** Per-claim grid via `claim_entities` with UNIQUE (claim_id, tile_x, tile_y). No global tile grid by design — keeps world-geometry pathing clean. `factory-cycle` heartbeat advances belts (1 item per tick to first connection) and crafters (consume all recipe inputs → produce output). Kill-switch `CONCORD_FACTORY_ENABLED=0`.
  - **Time loops scope by (user, world).** Same world can't have two active loops at once; different worlds can each have one. `loop_memories.retained_across_loops = 1` is the default — memories survive the rewind. `inventory_snapshot_json` + `position_snapshot_json` are restored on next `startLoop` in the same world.
  - **Asymmetric horror inverts Sovereign Raid.** One `ghost_user_id` cannot also `joinAsInvestigator`. `EVIDENCE_TO_WIN = 3` distinct sighting kinds → investigators win. `downInvestigator` is ghost-only; all-downed → ghost wins. Both win-paths set `ended_at` + `end_reason` atomically.
  - **Theme park appeal compounds with visits** at `APPEAL_RIDE_BONUS_PER_VISIT = 0.001`, capped at 1.0. Visitor satisfaction grows by `SATISFACTION_GAIN_BASE = 0.15` per ride. Departing visitors (past `leaves_at`) decrement `current_visitors` on the parent attraction.
  - **Extraction runs drop loot as `lost_loot_json` on death.** The corpse-drop hook to mig 151 `player_corpses` is the caller's responsibility (kept decoupled). Extraction zone radius check is Euclidean.
  - **CF surfaces are read-only or behind auth.** All 11 CF route batches preserve existing semantics — no new substrate, only HTTP surfaces. `getPuzzle`-style explicit-column safety applies to anything sensitive (solution paths, secrets).

- **Belonging sprint invariants (Phase BA-BE):**
  - **Houses inherit world destructibility.** A house is a `world_buildings` row with a `player_houses` linkage; combat inside applies `applyStructuralStress` (Layer 7.5) the same as it would anywhere else. No "house immunity" mode — by design.
  - **Houses lock via `world-crime.js`.** Lockpicking on a house door is the same skill check as on any building room (`attemptLockpick` against `building_rooms.lock_tier`). Force entry damages the door + emits evidence. `setLockTier` in `player-housing.js` writes through to the existing column so no parallel lock substrate exists.
  - **Furniture placement is per-coord JSON.** `building_rooms.furniture_layout_json` carries `[{itemId, x, y, z, rot}]` per room. The flat string-list `furniture` column is preserved for back-compat — `getHouse` returns both. New code writes the layout; old readers see the old shape.
  - **House snapshot debounces on edits.** `placeFurniture`, `removeFurniture`, and `setVisibility` all call `scheduleSnapshotCapture(db, houseId)` which collapses rapid edits into a single 5s-debounced `captureSnapshot` writing into `player_houses.snapshot_json`.
  - **Wardrobe overlay is non-destructive.** `equipOutfit(db, outfitId, userId, 'cosmetic')` (default) writes to `users.cosmetic_wardrobe_overlay_json`; underlying stat gear stays in `appearance_json`. Renderer pipeline: base → wardrobe overlay (BA4) → dye overlay (BA3). Old `mode='replace'` path remains for back-compat.
  - **Dyes are per (user, avatar, slot, channel).** 4 channels per slot: primary/secondary/trim/glow. `applyAppearanceOverride` is a PURE compose function — no DB access — so the renderer can call it inline every frame.
  - **Scars + drift feed the avatar render.** `useAvatarScars(playerAvatar.id)` polls `/api/avatars/:userId/scars` + `.../drift` (public read), captures into `wearUniformRef` for the per-frame render block. Drift score (0..1) drives the `u_wear` shader uniform; scars list maps to DecalGeometry on the matching bone region.
  - **Festivals are calendrical, not ruler-issued.** `festival-trigger-cycle` heartbeat (freq 4) checks `(season_idx, day_in_season)` against `festivals` rows + opens `festival_active(festival_id, world_id, year_idx)` idempotently. Kingdom-decree `festival` (24h popularity bump) is unchanged. Kill-switch `CONCORD_FESTIVALS_ENABLED=0`.
  - **Seasonal achievements stamp at unlock.** `season_idx` + `year_idx` captured from `_calendarSnapshot()` at the moment of `unlockAchievement`. PK is still `(player_id, achievement_id)` — you can't re-earn the same achievement across seasons (by design). `seasonOnly` + `festivalOnly` in achievement JSON gate unlocks.
  - **No battle pass exists by design.** User picked seasonal achievement gating over a pass ladder. `battle_pass`, `pass_tier`, `reward_track` tables are intentionally absent.
  - **Operator announcements broadcast via heartbeat.** `announcement-broadcaster` freq 60 scope:global dequeues unbroadcast rows (`last_broadcast_at IS NULL`) and emits `concord:announcement`. Admin-only POST; public read. Kill-switch `CONCORD_ANNOUNCEMENTS_ENABLED=0`.
  - **Guild substrate is DB-backed; org graph stays in-memory.** `world-organizations.js` (in-memory LruMap) remains the social graph. The new `org_progression`, `org_inventory`, `org_inventory_log` tables join by `org_id`. Role gating in the new lib uses caller-supplied predicates (`isMember`, `isOfficer`, `isLeader`) so the in-memory members map stays the source of truth.
  - **Guild halls are buildings with `owner_type='org'`.** `claimHallBuilding` transfers `world_buildings.owner_id = orgId`. Decoration uses the same Phase BA1 `furniture_layout_json` path — officers can place furniture in the hall via the housing routes (different gate predicate).
  - **NPC mentor profiles auto-derive.** Authored NPCs seeded via content-seeder; procgen NPCs auto-promote at `skill_revisions.revision_num >= 5` via `maybePromoteToMentor`. The crown badge is read-only client-side; the mentorship REQUEST goes through the existing mig-127 `requestMentorship` path.
  - **World bosses lock out per (user, schedule).** `defeatBoss` applies a `world_boss_lockouts(user_id, schedule_id)` row with `locked_until = now + tier.lockout_h`. Different schedules don't share lockouts. Kill-switch `CONCORD_WORLD_BOSSES_ENABLED=0`.
  - **Boss scheduler uses wall-clock for the encounter window.** `runTriggerPass` accepts a logical `now` for trigger comparison but writes `opened_at`/`closes_at` from `Date.now()` so `listActiveBosses`' `unixepoch()` filter returns rows correctly.
  - **Difficulty tiers gate by prerequisite chain.** Mythic requires heroic clear of THIS encounter; heroic requires normal; normal requires finder. Per-encounter chains; no global "level requirement." `applyDifficulty` is a PURE compose that scales damage/health/loot.
  - **Event cascades are bounded.** Max depth 10. `triggerCascade` is idempotent on `(parent_event_id, outcome)` — re-realising never double-spawns. Child quest id is deterministic by default so re-trigger maps to the same id.
  - **Photo gallery photos are DTUs when shared.** `sharePhoto` mints `kind='photo'` + flips visibility to public. The royalty cascade fires when another player cites the photo. Blob storage under `./data/photos/{photoId}.png`; 5MB cap per photo.

- **Audit-grounded depth sprint invariants (Phase AA-AG):**
  - **Cross-world royalty cascade is intentional.** Migration 225 added `dtus.world_id`; `server/economy/royalty-cascade.js#registerCitation` looks up parent + child `world_id` and stamps `crossWorldHop = true`, `parentWorldId`, `childWorldId` onto each ledger entry's metadata. The royalty math is world-agnostic by design (a citation is a citation); what changed is observability. Lookup is best-effort try/catch and never blocks the cascade — minimal builds without the `world_id` column degrade gracefully.
  - **Avatar Web Worker IS wired.** `concord-frontend/components/world-lens/AvatarSystem3D.tsx` imports `useAvatarAnimator` + `serializableToGaitPose` and routes both gait call sites (player + NPC) through `avatarAnimator.requestGait(...)`. Inline `synthesizeGait` is the deterministic fallback when the worker hasn't returned a pose. Don't reintroduce a pure-inline gait path — the pre-AA2 code stalled the React main thread under 5+ visible NPCs.
  - **Nemesis NPC↔NPC graph is per-world.** `npc_nemesis` rows scope by `world_id` (migration 226 — the table is `npc_nemesis`, NOT `npc_relationships`; the latter name belongs to migration 062's family/bond graph and the two collided on `CREATE TABLE IF NOT EXISTS` until the rename, which had silently dropped the nemesis table's columns + indexes). Cross-world NPC relationships don't exist by design — an NPC lives in exactly one world. The (a, b, kind) UNIQUE constraint with CHECK `(npc_a_id < npc_b_id)` enforces sorted-pair canonicalization; `formRelationship` auto-sorts. Heartbeat `nemesis-cycle` (freq 40, scope: 'world') runs three rules (kin grief bond, scheme betrayal + opinion < -50 → rival, mentor pairing on level gap ≥ 15) plus a 60-day decay sweep. Kill-switch `CONCORD_NEMESIS_CYCLE=0`.
  - **Buy orders escrow up-front, refund unfilled on cancel/expire.** `server/lib/auctions.js#placeBuyOrder` walletDebits `unitPrice × quantity` immediately; `cancelBuyOrder` and `sweepExpiredBuyOrders` refund only the unfilled portion (filled portion already paid out to sellers). Self-fill is rejected. The PRIMARY KEY pattern on `auction_buy_orders` is the same single-transaction contract as the sell side.
  - **Disease transmission probabilities live in catalog JSON.** Each disease's `transmissionProbabilities` + `vectorRequirements` override the `DEFAULT_TRANSMISSION` table in `server/lib/disease-engine.js`. Default uniform probability applies if the JSON omits the field — back-compat with existing 19 diseases. Vector gates: airborne distance-decays + hygiene-modulated, touch requires zero distance + hygiene-modulated, foodborne requires contamination ≥ 0.2, bloodborne requires open wound, waterborne requires contaminated source intersect. Hygiene at 1.0 halves touch + airborne contraction.
  - **Quest dialogue compose is opt-in.** `CONCORD_QUEST_DIALOGUE_LLM=true` routes through the subconscious brain via `server/lib/quest-dialogue-composer.js#composeQuestDialogue`. The deterministic fallback ALWAYS works — env-disabled, LLM-throw, parse-failure, and 8s timeout all fall back to a seeded 3-line composer keyed by (questId, npcTone, desire) so same NPC always picks the same opener for the same quest. Dialogue persists into `lattice_born_quests.dialogue_json` (migration 229).
  - **Training-room frame data is derived, not stored.** `server/lib/combat-frame-data.js#getSkillFrameData` reads from the DTU substrate; no new tables. Bow + staff have `parry_window_ms = 0` by design (ranged weapons don't parry). Level scaling caps at 30% faster startup/recovery at level 100. Route `GET /api/combat/frame-data/:skillId` is public-read.
  - **Ambient chat is per-district.** `ambient_chat_messages` (migration 231) keys by `(world_id, district_id)` and the list query filters on both — same district name in different worlds is intentionally separate. Cross-district visibility is zero by design — encourages district identity (Bakharev society-of-spectacle). Rate-limit 5 msg / 60s per user; 280-char cap; 1h default TTL with sweep heartbeat (`ambient-chat-sweep` freq 60 scope: 'global'). Kill-switch `CONCORD_AMBIENT_CHAT_ENABLED=0`.
  - **38 authored achievements.** Combat 9 + economy 6 + exploration 7 + mastery 10 + social 6 = 38 (post-AA4). Add new ones to the appropriate `content/achievements/<category>.json` array; the loader auto-discovers.

- **Production sprint invariants (Phase D):**
  - **The world lens is the canonical surface.** Every gameplay feature must be reachable from inside `/lenses/world` without navigation away. Standalone lenses may coexist as sidebar entries (e.g. `/lenses/courtship`, `/lenses/garage`, `/lenses/creatures`, `/lenses/fishing`, `/lenses/reasoning/traces`) but the world-lens path MUST work first.
  - **NPC click → branching menu, not dialogue-only.** `NPCActionMenu` (DA1) surfaces 7 contextual actions: Talk / Mentor / Brawl / Court / Inspect Traits / Trade / Hire. "Talk" forwards to the legacy `concordia:open-dialogue` event for back-compat. New NPC interactions add a row to the menu — do not bypass it.
  - **Building interaction is proximity-gated via DA2 router.** `StationInteractionRouter` listens for `concordia:building-interact` events, looks up `building_type` against `ROUTER_TABLE`, and mounts the matching overlay. Players must be within 4m of a building (Math.hypot check) before the router fires. Map keys: `farm_plot`, `restaurant`, `karaoke_booth`, `mahjong_table`, `trivia_kiosk`, `hacking_terminal`, `programming_console`, `factory_workbench`, `attraction_booth`, `creature_pen`, `glyph_altar`. To add a new station, extend `ROUTER_TABLE` + add the `building_type` to `server/lib/building-interiors.js` ROOM_TEMPLATES.
  - **Ctrl+K opens the global command palette.** `CommandPalette` (DA3) binds Cmd/Ctrl+K, fuzzy-matches against the lens registry + 10 curated world actions. Keyboard binding is sacred; don't rebind.
  - **Run-mode entry comes through DA4 hotbar group.** Six modes: roguelite, horde, extraction, horror-ghost, time-loop, brawl. Each click opens a confirm modal then POSTs `/api/{mode}/start`. The palette also dispatches `concordia:start-mode` events that the hotbar consumes.
  - **Farm + restaurant building wrappers are owner-gated.** `/api/farming/building/:id/plant`, `.../harvest`, `/api/restaurant/building/:id` all check `world_buildings.owner_type === 'player' && owner_id === userId` and treat `building.id` as the claim_id (farm) or auto-open a restaurant for the owner (restaurant). The original /api/farming/plant and /api/restaurant/open routes still work for direct land-claim integration.
  - **Canonical combat is Skyrim-style action, not RTwP.** See the Convergence-sprint invariants for the full ground truth — `CombatInputController.tsx` keyboard input → real-time socket events, procedural biomechanics + PD motors + impact-momentum resolution, no AABB hitboxes. The `PartyCombatHUD` (DB9) is a SEPARATE optional tactical layer for `party_combat_sessions` only; it polls `findActiveSessionForPlayer(db, userId)` at 1Hz and `/tick` + `/state` at 5Hz, pauses via `setTimeScale(0)`. The HUD is dormant unless the player is inside a party-tactics session. The orphaned `turn_combats` tables from migration 251 are harmless dead substrate — do NOT write to them.
  - **Hacking `getPuzzle` does NOT leak `solution_path_json`** — explicit column list at `server/lib/hacking.js:108`. The HackingTerminal (DB10) does local FS resolution against `terminal_tree` for ls/cd/cat (no server round-trip) and POSTs every command to `/api/hacking/:id/command` for solution-path tracking.
  - **Code puzzle submit is all-or-nothing.** `CodePuzzleEditor` (DB11) gates the Submit button on `result.passed === true` from a prior /run call. The server-side `submitSolution` ALSO re-verifies all test cases — never trust the client's `passed` flag.
  - **Factory entities live inside `land_claims` only.** No global tile grid by design. `FactoryEditor` (DB12) calls `lensRun('land_claims', 'list_for_user')` to discover owned claims first, then queries `/api/factory/claim/:claimId` per-claim. Left-click empty places (after type pick); left-click entity selects for connect; right-click removes.
  - **Time loop tint is shader-driven.** `TimeLoopHUD` (DB13) dispatches `concordia:world-tint` events with `{ source, color, intensity }` as the loop nears expiry. The world's existing shader pipeline consumes these.
  - **Asymmetric horror role HUDs are mutually exclusive.** `findActiveSessionForUser` returns `{ role: 'ghost' | 'investigator' }` and the HUD branches on `role`. `ghost_user_id` cannot simultaneously be in `investigators_json` — server enforces.
  - **Extraction zone overlays render in 3D, not as 2D HUD.** `ExtractionRunHUD` (DB16) uses `window.__concordiaPlayerPos` (set by AvatarSystem3D) to compute distance to active zones; the Extract button is gated on `distance <= zone.radius_m`. Bottom-center pill is a status display, not the interaction surface.
  - **DC1 sports lens calls the engine.** The existing 1056-LOC `/lenses/sports/page.tsx` now has a "Leagues (live)" tab (4th tab) that mounts `LeagueStandings` + `MatchSimulator` — these components hit `/api/sports/league/*` directly. Production gate: the new tab must show real league data.
  - **Courtship 3D overlay reads `partner_kind === 'npc'`.** `CourtshipProgressOverlay` filters `/api/courtship/mine` to NPC partners only, draws a heart icon + affinity % above their head via the existing `concordia:projector-ready` 3D-to-screen pattern. Wedded NPCs show ⚭ in amber instead of ♥ in pink.
  - **Karaoke microphone uses real Web Audio.** `KaraokeMicrophone` (DC4) requests `getUserMedia({ audio })`, runs autocorrelation pitch detection over a 2048-sample buffer, aggregates pitch deviation + rhythm jitter, and submits to `/api/karaoke/resolve`. The grade (S/A/B/C/D) comes from the server. Don't fake the mic — the resolver expects real signal.
  - **Mahjong now tracks real tiles (was a checkbox).** A real tile engine exists — `server/lib/mahjong/session.js` deals a seeded wall, tracks 4 seats, runs 3 NPC discard AIs (`npc-discard.js`), validates tsumo via `isWinningHand`, and `yaku-detect.js` does genuine 14-tile decomposition for 9 yaku. The live UI (`MahjongTable.tsx`) uses the `/api/mahjong/start|discard|tsumo|state` routes. The legacy checkbox path (`minigame-resolvers.js#resolveMahjongHand` + `/api/mahjong/resolve` + `mahjong.resolve_hand` macro) is **superseded dead code** — it still sums `MAHJONG_HAND_VALUES` over a player-supplied yaku list with no verification; retire it so it can't be called. Real engine gaps (self-documented): no chi/pon/kan, no riichi, tsumo-only.
  - **Creature breeding is environment-aware.** `CreatureBreedingPanel` (DC6) passes both creatures' `species_id` + `lifestyle` + the shared `biome` (when same) to `/api/creatures/breed`. `sameEnvironmentBonus: true` when both share a biome — the lib bumps compat score.
  - **Footprint overlay is skill-gated.** `/api/tracking/recent/:worldId` returns `{ gated: true, requiredLevel: 5 }` until the player's `tracking_skill_xp.level >= 5`. The FootprintLayer renders nothing while gated — no client-side bypass.
  - **Drift toast surfaces HIGH/CRITICAL only.** `DriftAlertToast` (DC7) polls `/api/drift/alerts?severity=HIGH&severity=CRITICAL`, dedupes by `seenIds`, surfaces one at a time. Click "Resolve via constraint check" POSTs `/api/reasoning/run` with `mode=constraint_check` — the HLR engine records a real reasoning trace.
  - **Trait inspector dispatches via two event names.** `NPCTraitInspector` (DC14) listens for BOTH `concordia:open-trait-inspector` AND `concordia:inspect-npc-traits` (the event the DA1 menu dispatches). If you add a new entry point, prefer `concordia:open-trait-inspector` for new code; the alias exists for back-compat.
  - **Phase D E2E lives at `server/tests/e2e/production-sprint.test.js`.** 27 structural assertions — every DB/DC mount + every new HTTP endpoint + every new lib helper. Run via `node --test server/tests/e2e/production-sprint.test.js`. If a wire is missing, the test fails before anything else does.

- **Marketplace fees are hardcoded.**
  - DTU royalty-aware path (`/api/marketplace/purchaseWithRoyalties`): `creatorPool = price * 0.95`, `platformFee = price * 0.05` — 95% to creator pool.
  - Economic marketplace path (`/api/economic/marketplace/buy`): `MARKETPLACE_FEE: 0.04`, `CREATOR_SHARE: 0.70`, `ROYALTY_SHARE: 0.20`, `TREASURY_SHARE: 0.10`.
  - Token purchase fee (Stripe → Concord Coin): `TOKEN_PURCHASE_FEE: 0.0146`.
  - Creative marketplace constants (`server/lib/creative-marketplace-constants.js:422-440`): `PLATFORM_FEE_RATE 0.0146`, `MARKETPLACE_FEE_RATE 0.04`, `INITIAL_ROYALTY_RATE 0.21`, `ROYALTY_HALVING 2`, `ROYALTY_FLOOR 0.0005`, `MAX_CASCADE_DEPTH 50`.
  - Royalty cascade cap (`server/economy/royalty-cascade.js:180`): `MAX_ROYALTY_RATE = 0.30` of the sale price to ancestors. Seller always keeps ≥64.54% (100% − 5.46% fees − 30% royalty cap). Tested in `tests/royalty-cascade.test.js`.
  - **Do not change any of the above without governance approval.** They are constitutional invariants.
- **Earned-only, 48-hour-held CC withdrawals.** `server/economy/withdrawals.js#earnedWithdrawableBalance` (shared by `requestWithdrawal` + `creator-dashboard.js#computeWithdrawalEligibility`) — ONLY **earned** CC is withdrawable to fiat: marketplace-sale seller credits + `ROYALTY_PAYOUT`, settled ≥ `WITHDRAWAL_HOLD_HOURS = 48`. Purchased CC (`TOKEN_PURCHASE`) is closed-loop, spend-only store credit and can NEVER be cashed back out — the one-directionality (deposit closed-loop, only creator earnings redeem) is the money-transmitter risk-reducer (Roblox Earned-Robux/DevEx shape). Peer `TRANSFER`-in is deliberately NOT earned (anti-launder). The 48h hold is also the anti-refund-exploit gate (sell → withdraw → buyer refund → funds gone). `WITHDRAWABLE_EARNED_TYPES = ["MARKETPLACE_PURCHASE","ROYALTY_PAYOUT"]`. Tests seed EARNED rows with backdated `created_at`; `tests/economy/withdrawal-earned-policy.test.js` pins the policy.
- **Ledger credits are summed via `CREDIT_ROW_PREDICATE`.** `server/economy/balances.js` exports the canonical predicate that EXCLUDES the redundant debit-half rows of the two-row `TRANSFER`/`MARKETPLACE_PURCHASE` pattern (those carry both `from_user_id` and `to_user_id`; the matching `from IS NULL` credit row is the real credit). Summing `net` for every `to_user_id` row double-credits the recipient and mints CC. ALL seven credit summers use it (`balances`, `coin-service` treasury invariant, `treasury-reconciliation`, `routes` integrity, `lens-culture`, `creator-dashboard`, `account-lifecycle`). New balance math MUST apply it. Pinned by `tests/economy/ledger-conservation.test.js`.
- **Citation requires consent.** `server/economy/royalty-cascade.js:registerCitation` short-circuits with `citation_consent_not_granted` unless one of: parent DTU is public/published/global-scoped, parent creator toggled `allow_citation`, OR caller holds a purchased usage license. Tests must pass `parentDtu: { visibility: "public" }` or `hasPurchasedLicense: true` to exercise downstream cycle/persistence logic.
- **Heartbeat modules must never throw.** Always wrap in `try/catch`.
- **DTU originals are tombstoned by the forgetting-engine retention pathway** (`server/emergent/forgetting-engine.js:134, 155`), preserving lineage. The user-initiated `dtu:deleted` event hard-deletes; do not extend hard-delete paths to retention sweeps.
- **NPC secrets (narrative_context.secret) must not be passed to LLM prompts.** They are for human authors and branch conditions only. The narrative bridge enforces this at `server/lib/narrative-bridge.js:184` (structural omission) + `:220` (defense-in-depth canary scan); the invariant is documented at `:105`.
- **DTU consolidation is automatic** via inline pipeline in `governorTick` at `server.js:31290` (every `TICK_FREQUENCIES.CONSOLIDATION = 30` ticks). Forms MEGAs from regular DTU clusters (size 5–20), then HYPERs from MEGA clusters (size 3–10) once MEGA population ≥ 15. Constants at `server.js:2118-2156` (`TICK_FREQUENCIES`). Manual `compressToDMega()` / `compressToHyper()` (`server/economy/dtu-pipeline.js:327, 401`) still callable from macros.
- **Refusal Field glyph algebra is load-bearing.** `server/lib/refusal-field.js:applyTemporaryRefusal` (lines 59–76) computes a real per-entry glyph from `computeBase6Layer` + `glyphDiv`; `computeFieldComposition` accumulates them via `glyphAdd` (lines 175–222) and returns a `strength` numeric. Callers MUST branch on `strength >= 6` (`isCompoundRefusal`) for compound-refusal mechanics: Concordia goddess "deep cold" dialogue tone (`server/routes/world-narrative.js`), world-event suspension, and the dome-collapse Mass Raid phase (`server/lib/sovereign/raid-event.js`). Strength is hard-capped at 9. The May 2026 audit fixed two latent bugs that had silently kept `composedFrom` at 0 (calls to `.value` on objects that don't have one); regression tests live at `tests/refusal-algebra/strength-gating.test.js`. Time-bounded gates from the `FIELD_KINDS` table (death/harvest/hostility/consequence/numbers/dome/win) ALSO still enforce per-kind blocks via `isRefused()`.
- **Combat damage and reach are server-validated.** `routes/worlds.js#_validateCombatReach` and `_validateDamageCap` gate `/api/worlds/:worldId/combat/attack`. Reach uses the live `cityPresence` position; declared `skill.range_m` is capped at 80m globally. Damage is capped at `skill.max_damage * 2.5` or 500 hard. Never reintroduce a "trust the client damage field" path — the prior unguarded code was a real one-shot / cross-map hack risk and is regression-tested at `tests/combat-anti-cheat.test.js`.
- **Player inventory is USER-GLOBAL.** All `player_inventory` reads/consumption MUST scope by `(user_id, item_id)` — **NOT** by `world_id`. It's one universe with many worlds connected by the **Concord Link**; a player carries their inventory between worlds, so an item earned anywhere is usable in every world and every lens (this is the keystone the cross-world economy / factions / wars / schemes depend on). The global PK `(user_id, item_id)` means exactly one row per item; `world_id` (migration 101, default `concordia-hub`) is **acquisition metadata only, never a visibility filter**. Per-world differentiation lives in item **EFFECTIVENESS/POTENCY** (`lib/embodied/skill-environment.js#elementalEnvBoost`/`terrainResourceBoost`), not ownership. New code that adds `AND world_id = ?` to an inventory **read** strands items in the world they were earned — the `invariant-guardian` detector (`player_inventory_user_global`) flags it. (This reverses the earlier, mistaken "per-world inventory" invariant; migration 101's framing and the old detector rule were the bug, not the fix.)
- **World-event rewards are minted, not pretended.** `lib/world-events.js#endEvent` is async and MUST call `mintCoins` with `event_reward:${eventId}:${userId}` refId. Pre-fix code path emitted only realtime toasts (`event:reward` socket event) — the wallet never updated. Idempotency lives in the refId; a re-run of `endEvent` won't double-mint.
- **All LLM system prompts live in `server/lib/prompt-registry.js`.** Never inline a system prompt at the call site. The registry exports three things: `BRAIN_IDENTITY` (per-brain functional directive — `conscious` is light because the Modelfile carries the persona, the other three brains get full personas because they have no Modelfile), `composeSystemPrompt(brain, ctx)` (per-brain merge that returns `{ system, useModelfileSystem }` — for `conscious`, the Modelfile SYSTEM block is the persona and the `system` field carries functional + runtime context only), and `TASK_PROMPTS` (28 specialized one-shot prompts as functions: expertMode, agentMode, teachingTutor, socraticTutor, patientTutor, npcDirective, councilMember, personaExpert, thoughtImprover, crispStructured, translation, creti, sharedConversation, morningBrief, subconsciousBridge, futureSim, cognitiveClone, ruthlessAdversary, resonanceAmp, critic, ethicist, auditor, knowledgeOrganism, challenger, engineer, synthesizer, historian, musicGenreCoach). New code that adds an inline `"You are..."` constant at a call site is wrong — the May 2026 audit found 30+ scattered inline prompts including a `_baseSystem = "You are ConcordOS..."` at the chat path that was silently overriding the Modelfile homeboy voice for every user. Add new prompts to the registry; reference them by function call.
- **DTU citation rule (conscious brain).** `BRAIN_IDENTITY.conscious` in `prompt-registry.js` instructs the brain to name a DTU briefly when one directly grounds a claim, but NOT to pad responses with citations, recite titles for show, or list DTUs as a substitute for actually answering the question. Pre-fix prompts told the brain to cite-everything, which caused every chat reply to read as a bibliography. If you find a test or downstream prompt fighting this rule, the test/prompt is stale — citations are surgical, not decorative.
- Migrations are append-only. Never modify an existing migration file. **One audited exception** (2026-05): migrations 209/213/226 were edited to rename three colliding tables (`bounties`→`crime_bounties`, `tournaments`→`world_tournaments`, `npc_relationships`→`npc_nemesis`). Each collided with an earlier `CREATE TABLE IF NOT EXISTS` of the same name, so the later table + its indexes were never created on ANY DB and `runMigrations` (which halts on the first throw) silently stopped applying everything after the failure. Because no DB ever held the failed-migration's intended schema, the edits change nothing for already-migrated DBs — they only let fresh installs apply cleanly. Migration **275** forward-repairs existing installs (rebuilds `evo_asset_interactions`/`evo_asset_versions` FKs that migration 202 left dangling at the dropped `evo_assets_v2`, and restores the `train_consented` column 202 dropped). Don't treat this as license to edit migrations casually — it was a fix for genuinely-broken, never-applied migrations.
- `CONCORD_NO_LISTEN=true` + `NODE_ENV=test` both suppress port binding for tests.
- The frontend `build` script runs `prophet-check` (repair cortex pre-build analysis) before `next build`. Build blockers will exit 1.

---

## Multi-tenant cap defaults (lifted from single-user pre-deploy)

Several caps were intentionally raised before multi-tenant deploy. If you find a test asserting the old value, the test is stale — update to the new default.

| Constant | Old | New | Env override |
|---|---|---|---|
| `MAX_DOMAIN_SIGNALS` (`server/lib/session-context-accumulator.js:29`) | 50 | **500** | `CONCORD_DOMAIN_SIGNALS` |
| `MAX_ACTIVE_LENSES` (same file:30) | 15 | **175** | `CONCORD_ACTIVE_LENSES` |
| `MAX_SESSION_HISTORY` (same file:28) | 30 | **300** | `CONCORD_SESSION_HISTORY` |
| `MAX_CONCURRENT_DOWNLOADS_PER_USER` (`server/lib/storage-constants.js:210`) | 5 | **25** | `CONCORD_DOWNLOADS_PER_USER` |
| `SESSION_LIMITS.MAX_CONCURRENT` (`server/emergent/schema.js:98`) | 5 | **50** | `CONCORD_DIALOGUE_MAX_CONCURRENT` |
| `MAX_ARCHIVED_SUMMARIES` (`server/lib/conversation-summarizer.js:26`) | 20 | **200** | `CONCORD_ARCHIVED_SUMMARIES` |

---

## Phase D first-draft constants (untuned)

These dials shipped with Phase D + Z and have NOT yet been playtested.
A future "Phase E — Balance" sprint should walk through each, pin
real numbers from observed play, and either bake the result in or
expose an env override. Listed for grep:

| Constant | File | Effect when low | Effect when high |
|---|---|---|---|
| `POLL_MS = 1000` | `HordeWaveHUD.tsx` | tighter live wave count | more wasted network |
| `POLL_MS = 2000` | `ClimbingTracker.tsx`, `TimeLoopHUD.tsx`, `ExtractionRunHUD.tsx` | snappier HUD updates | more wasted network |
| `POLL_MS = 2500` | `HorrorRoleHUDs.tsx` | tighter ghost/investigator state | more wasted network |
| `POLL_MS = 3000` | `RestaurantDashboard.tsx`, `ThemeParkAttractionPanel.tsx` | tighter order/visitor sync | more wasted network |
| `POLL_MS = 15_000` | `DriftAlertToast.tsx` | faster surface | more wasted network |
| `POLL_MS = 30_000` | `CourtshipProgressOverlay.tsx`, `FootprintLayer.tsx` | fresher data | more wasted network |
| `DISCOVERY_MS = 1000`, `TICK_MS = 200` | `PartyCombatHUD.tsx` | snappier combat polls | more wasted network |
| `FRAME_THROTTLE_MS = 100` | `CourtshipProgressOverlay.tsx` | smoother projection | more frame work |
| `FRAME_THROTTLE_MS = 200` | `FootprintLayer.tsx` | smoother | more work |
| `GRID_W = 5, GRID_H = 5` (farm) | `FarmTileEditor.tsx` | tight grid | sprawling farms |
| `GRID_W = 10, GRID_H = 8` (factory) | `FactoryEditor.tsx` | constrained design | open canvas |
| `DEFAULT_ORDER_TTL_S = 5*60` | `restaurant.js` | frantic service | leisurely |
| `TIP_FRACTION_FAST = 0.20` (T3.4 adopted from G3.1 sim) | `restaurant.js` | weak speed incentive | strong |
| `TIP_FRACTION_OK = 0.15` (T3.4 adopted from G3.1 sim) | `restaurant.js` | mild reward | strong |
| `EVIDENCE_TO_WIN = 3` | `horror.js` | brisk investigators | grindy investigators |
| `DEFAULT_DURATION_S = 25 * 60` (horror) | `horror.js` | tense session | drags |
| `DEFAULT_LOOP_DURATION_S = 30 * 60` | `time-loop.js` | rapid loops | long loops |
| `MAX_CYCLES = 10_000` | `programming-puzzle.js` | hangs at low | wasted compute at high |
| `MAX_ROYALTY_RATE = 0.30` | `royalty-cascade.js` | thin ancestor share | seller shrinks |
| `WITHDRAWAL_HOLD_HOURS = 48` | `withdrawals.js` | refund-exploit window opens | UX friction |
| `MIN_AFFINITY_TO_PROPOSE = 0.60` (hardcoded in lens) | `app/lenses/courtship/page.tsx` | too easy | grindy |
| `VISIBLE_RADIUS_M = 16` | `CourtshipProgressOverlay.tsx`, `NPCActivityTag.tsx` (12m) | overlay flickers | floods screen |
| `RECOVER_RADIUS_M = 4` | `player-corpse.js` | too tight | too lax |
| `MAX_ACTIVE_PER_USER = 50` (signs) | `player-signs.js` | spam-prone | UX clutter |
| `PLACE_COOLDOWN_S = 60` (signs) | `player-signs.js` | brisk | spam-prone |
| `SIGN_TTL_DAYS = 7` | `player-signs.js` | ephemeral | persistent clutter |

These are not invariants — they're playtest fodder. A future balance
pass would visit each and ship a tested value.

---

## Test suite

- `npm test` runs `npm run test:main && npm run test:behavior`, where `test:main` is `NODE_ENV=test node --test --import=./tests/preload/no-egress.mjs --test-timeout=300000 'tests/**/*.test.js' 'tests/**/*-tests.js'` (per `server/package.json`, read directly — not hand-typed). **`--test-force-exit` was REMOVED (commit `0b0c11df`) and the timeout is 300000ms, not 60000ms — both stale in earlier text of this section.** See the `--test-force-exit` entry below for why the flag could be dropped.
- **Current measured totals (2026-07-25, flagless `npm run test:main`, real 38m23s): 37,622 tests / 37,613 pass / 0 fail / 4 cancelled / 5 skipped.** This is the load-bearing snapshot — supersedes the "36,510 / 8 fail" figure that used to sit here (that run predates the `--test-force-exit` removal below and was mid-investigation, not a stable baseline). **Zero test failures.** The run still exits 1, because of the 4 cancellations: `tests/coverage-smoke-routes-lib.test.js`, `tests/coverage-smoke.test.js`, `tests/mind-space.test.js`, `tests/platinum-chaos-heartbeat.test.js`, each a genuine file-level 300s timeout. `coverage-smoke-routes-lib.test.js` was independently re-run ALONE on an otherwise-idle box and still cancelled at 301s (141 pass / 0 fail / 1 cancelled) — ruling out cross-file contention as the cause; these are real never-completing subtests (an earlier hypothesis blaming contention was wrong), still open. Budget ~40 minutes for a full run.
- **🔴 `process.exit()` inside a `node:test` `after()` hook SILENTLY SWALLOWS FAILURES — fixed 2026-07-25, know the shape.** It pre-empts node:test's completion path before `not ok`/`# fail` or a non-zero exit code can be emitted — the run exits 0, indistinguishable from a genuine pass. Reproduced on a bare `assert.equal(1,2)`: synchronous `process.exit(0)` in `after()` → `# pass 1 / # fail 0` / exit 0; an unref'd `setTimeout` watchdog instead → correctly `not ok 1` / exit 1. Fix: decouple the exit via an **unref'd watchdog**, never a bare synchronous `process.exit()`. `tests/lib/server-clean-exit.js` had drifted out of sync and carried the bug until 2026-07-25 (all 13 `registerServerCleanExit` callers affected, now fixed) — if a suite's pass count looks suspiciously clean, check its teardown first.
- **`--test-force-exit` was REMOVED 2026-07-25 (commit `0b0c11df`) and is no longer load-bearing.** It used to mask a real open-handle leak (worker + fallback timer both `.unref()`'d meant the event loop wound down before the worker's buffered `exit` event was ever delivered, leaving teardown permanently pending) rather than hide missing tests — a controlled A/B confirmed test *counts* matched within 2.8% with/without the flag. Root-caused and fixed at the three sites with the pattern (`workers/heartbeat-pool.js`, `workers/macro-pool.js`, `server.js#terminateCognitiveWorkerForTest`); a related **disk** leak (7 e2e suites never cleaning up their `mkdtemp` SQLite dirs, ~800MB/run) was fixed the same day. Current flagless numbers are the measured-totals bullet above.
- **🔴 A timed-out e2e file ORPHANS its spawned server unless it arms the guard (2026-07-25).** `node --test` SIGTERMs a file that blows its timeout; with no listener, `after()` never runs and `process.on("exit")` never fires, so any `spawn()`'d `server.js` leaks (measured: ~111% CPU / 5.8GB RSS still running 7 minutes later) — self-amplifying, since the orphan's CPU load causes *other* files to time out too. Fix: `server/tests/lib/e2e-orphan-guard.js#armOrphanGuard(child, dataDir)`, called inside every e2e file's `spawnServer`. Its `process.exit(1)` lives in a signal handler (not the `after()`-hook hazard above) — installing the listener suppresses Node's default terminate, so the handler must itself exit or the runner's kill is silently ignored. Every new e2e spawner must arm this guard.
- **`test:e2e`'s timeout was 60s for a file whose own subtest needs ~105s — fixed 2026-07-25 → `--test-timeout=300000`, matching `test:main`.** Was a script bug, not a `--test-force-exit` masking artifact (a controlled pair confirmed the same failure independent of that flag). Post-fix: **205/205 pass, 0 cancelled, ~140s, exit 0.** Left the 105s alone deliberately — the required assertion resolves fast, but the bonus assertions gate on a full detector sweep (>2 min in this environment) that's worth keeping opportunistic rather than trimming away.
- Historical pre-force-exit-fix snapshot (Phase Z, 2026-05-28, 20,495 passing / 23 nominal "failing"): those 23 were green LLM-skips (`CONCORD_BEHAVIOR_TEST_LLM` gate), not hard failures — a stale count some earlier text in this file's history took at face value. Superseded by the measured-totals bullet above; not independently meaningful now.
- Phase Z added 4 integration tests under `server/tests/integration/`: `sports-engine.test.js` (DC1 league→teams→match end-to-end), `party-combat-tick.test.js` (DB9 findActiveSessionForPlayer + tick), `factory-live-update.test.js` (DB12 place→connect→tick→item moves), `z2-seeders.test.js` (boot seeders persist all 4 substrates). All 83/83 passing in isolation.
- Tier-1 behavior harness (`tests/behavior/lens-behavior-smoke.behavior.js`) auto-derives one test per (domain, macro) from the live `MACROS` map; skips LLM-hint and destructive-hint macros (run with `CONCORD_BEHAVIOR_TEST_LLM=true` to include).
- Tier-2 contract tests pin load-bearing math (royalty cascade, DTU quality scoring, refusal-field strength gating, world-event scheduler cadence).
- Tier-3 E2E covers the cook → eat → fight → commune onboarding journey (`tests/e2e/first-cycle-journey.test.js`).
- `npm install` in `server/` is mandatory before first run — the codebase hard-requires `better-sqlite3`, `express`, `jsonwebtoken`, `yaml`, `uuid`. All declared in `package.json`; no native build dependencies on RTX-class boxes.

---

## Adding a New Lens

1. Create `server/domains/<name>.js` with exported macro handlers
2. Add domain to `server/lib/lens-manifest.js` and `server/lib/lens-features.js`
3. Create `concord-frontend/app/lenses/<name>/page.tsx`
4. Add route to `server.js` if the lens needs custom endpoints beyond the macro system
5. Run `npm run validate-routes` and `npm run score-lenses` to verify consistency

## Adding a Heartbeat Module

1. Implement module in `server/emergent/<module>.js`
2. Register in `server/emergent/module-registry.js`
3. Wire into `governorTick` in `server.js` at the appropriate tick frequency
4. Run `npm run check-deps` to validate no circular dependencies introduced

## Heartbeat dispatch contract (Phase A–F)

`registerHeartbeat(id, opts)` in `server/emergent/heartbeat-registry.js` accepts these flags:

- **Dispatch is STRICTLY SEQUENTIAL, always — there is no parallel default.** `tickAllRegistered()` in `server/emergent/heartbeat-registry.js` runs due modules one at a time in a plain `for…await` loop; its own inline comments say "one at a time, always — never Promise.all" and "no two modules EVER execute concurrently," even when several share a frequency and become due on the same tick. `serial: true` does NOT opt a module into a parallel-vs-serial choice — both the default-flagged group and the `serial: true` group are internally sequential; the flag only controls ORDERING (the `serial` group runs after the default group, in registration order), for modules that MUST observe another module's same-tick writes (e.g. `social-npc-bridge` → `npc-knowledge-bridge`, `signal-propagation-cycle` → `creature-flock-cycle`). The file's own stale header comment claiming a parallel default was corrected in commit `5254396e`; this doc previously repeated the same error — read `tickAllRegistered`'s doc comment directly if in doubt.
- `worker: true` — routes through `workers/heartbeat-pool.js`. The handler runs in a worker with a read-only `better-sqlite3` handle and side effects (DB writes, realtime emits) replayed on the main thread. Pure-compute heavy modules only — anything that depends on live STATE or live socket fan-out stays inline. Currently flagged: `refusal-field-sweep`, `faction-strategy-cycle`, `lattice-quest-cycle`, `lattice-drift-scan`, `lattice-breakthrough-pass`, `embodied-dream-cycle`, `forward-sim-cycle`.
- `scope: 'global' | 'world'` — Phase F sharding. Default `'world'`. `scope: 'global'` runs on the parent process even when `CONCORD_SHARD_WORLDS=true`; reserve for cross-world infrastructure (federation poll, drift-scan, social-NPC bridge, presence sweep, draft GC, metrics decay).
- **The timeout bound is weaker than "cancels."** Each handler races `CONCORD_HEARTBEAT_MODULE_TIMEOUT_MS` (default 30s) via `Promise.race` and increments `concord_heartbeat_module_timeout_total{module}` on a loss — but `Promise.race` can only resolve early for a handler that actually yields the event loop (i.e. `await`s at least once). A synchronous, CPU-bound handler runs the JS stack to completion before the timeout's own `setTimeout` callback can even fire, so for a genuinely synchronous hang this is a best-effort bound on cooperative handlers, not real preemption. `server/server.js` (~806-825) independently documents a live instance of this: the parent-only detector sweep "genuinely exceeds 30s" at Concord's repo scale, so the periodic heartbeat's own safety net was silently truncating every scheduled run before it could stash a result — the operator-triggered manual sweep route deliberately bypasses `runHeartbeatModuleNow`'s timeout race for exactly this reason. A timed-out or thrown module never blocks the rest of the tick.

## DB write-ownership rules (Phase F)

When `CONCORD_SHARD_WORLDS=true`, each forked world shard opens its own writeable `better-sqlite3` handle. WAL mode permits one writer per table at a time; to avoid contention, the rules below MUST hold. The canonical lists are in `server/lib/world-shard-protocol.js` (`PER_WORLD_WRITE_TABLES` / `USER_GLOBAL_WRITE_TABLES`):

- **Per-world tables** (write-owned by the world shard): `world_npcs`, `city_presence`, `npc_routine_state`, `world_events`, `world_buildings`, `world_seasons`, `season_events`, `npc_schedules`, `embodied_signal_log`, `creature_corpses`, `dreams`, `forward_predictions`, `faction_strategy_state`, `faction_relations`, `faction_strategy_log`, `lattice_born_quests`, `procgen_regions`, `procgen_region_visits`, `land_claims`, `quest_triggers`, `quest_trigger_visits`, `player_signs`, `player_corpses`, and the npc-economy / npc-asymmetry tables.
- **User-global tables** (write-owned by the parent process via HTTP routes): `users`, `user_wallets`, `user_active_effects`, `dtus`, `dtu_citations`, `economy_ledger`, `pain_signals`, `mentorships`, `npc_skill_acquisitions`, `player_inventory` (per-world by column, but writes flow through HTTP routes on the parent).

New code that writes to a per-world table from an HTTP route or from a `scope: 'global'` module is wrong: it will work in single-process mode but will race the shard writer once Phase F is enabled. Tag the module `scope: 'world'` instead, or push the write through `req.app.locals.io` events that the shard consumes.

## Phase D — brain endpoint scale-out

`server/lib/brain-config.js` accepts `BRAIN_<NAME>_URLS` (comma-separated) as a multi-endpoint alternative to the singular `BRAIN_<NAME>_URL`. `pickBrainEndpoint(brain)` rotates across endpoints and prefers the less-loaded one; `noteEndpointStart` / `noteEndpointFinish` track inflight + failure counts (surfaced at `/api/admin/brain-endpoints`). On RunPod with a single Ollama instance, leave the plural unset — the singular URL still works. Enable when you spin up a second Ollama process on the pod or run in multi-container mode.

## Admin telemetry surfaces

- `/api/admin/heartbeat-stats` — per-module p50/p90/p99/last/run-count
- `/api/admin/worker-stats` — macro pool + heartbeat pool utilisation
- `/api/admin/brain-endpoints` — per-brain endpoint inflight + failures
- `/api/admin/world-shards` (+ `POST .../:worldId/restart`) — shard status + manual restart
- `/api/worlds/:worldId/health` — public-safe shard status badge feed
- Lens: **`/lenses/ops-telemetry`** mounts all of the above with 5s auto-refresh.
