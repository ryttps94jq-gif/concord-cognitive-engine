# Concord Frontend - Production Configuration
# API URL left empty — requests use relative paths (/api/...) which Next.js
# rewrites proxy to the backend (BACKEND_URL, default http://127.0.0.1:5050).
# This avoids CORS issues and ensures cookies flow correctly (same origin).
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_SOCKET_URL=
